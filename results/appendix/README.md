# Appendix results

| File / directory | Content |
|---|---|
| `loss_ablation/` | 4 logical CSVs backing Table 9 (`tab:loss_table`): BYOL substitution check, native-loss verification, BehaView loss robustness, (a) vs (b) view-similarity diagnostic |
| `feature_ablation.csv` | HOFINET k-NN graph variants × 4 seeds (BEHAV / PURE / STRUCT / HYBRID / FEAT) |

Each CSV is produced by the matching script under `scripts/appendix/` or by the inline analysis described in the loss-ablation README.
