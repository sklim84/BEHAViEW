% \documentclass[sigconf,nonacm]{acmart}
\documentclass[sigconf,natbib=true,anonymous=true]{acmart}

\usepackage{multirow, multicol, caption}
\captionsetup[table]{skip=2pt}
\usepackage{array}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\usepackage{adjustbox}
\usepackage[ruled,noend]{algorithm2e}
\DontPrintSemicolon
\usepackage{algorithmic}
\usepackage{bm}
\usepackage[utf8]{inputenc}
\usepackage{kotex}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage[switch]{lineno}
\urlstyle{same}
\usepackage{siunitx}
\usepackage{rotating}
\usepackage{colortbl}
\usepackage{enumitem}
\usepackage{makecell}
\usepackage{amsmath}
\let\Bbbk\relax
\usepackage{amssymb}
\usepackage{amsthm}
\newtheorem{theorem}{Theorem}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\theoremstyle{definition}
\newtheorem{assumption}{Assumption}
\usepackage{tablefootnote}
\usepackage{graphicx}
\usepackage{placeins}
\usepackage{xurl}
\usepackage{xspace}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds, shapes.geometric, decorations.pathreplacing}
\newcommand{\model}[1]{\texttt{\small\nolinkurl{#1}}}
%%%%%%%%%%%%%%%%%%%%   CUSTOM COMMAND    %%%%%%%%%%%%%%%%%%%%%
\newcommand{\method}{\texttt{BEHAViEW}\xspace}
\newcommand{\hofinet}{ATNet\xspace} % anonymized dataset name for double-blind review
% \DeclareRobustCommand{\resultdep}[1]{\textcolor{blue}{#1}} % (deprecated)
\usepackage{pifont}
\newcommand{\cmark}{\ding{51}} % check mark
\newcommand{\xmark}{\ding{55}} % x mark
\newcommand{\RED}[1]{\textcolor{red}{#1}}
\newcommand{\BLUE}[1]{\textcolor{blue}{#1}}
%%%%%%%%%%%%%%%%%%%%   CUSTOM COMMAND    %%%%%%%%%%%%%%%%%%%%%

\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

\settopmatter{printacmref=false}
% \renewcommand{\footnotetextcopyrightpermission}[1]{}
% \pagestyle{plain}

%% Conference information
% [CIKM submission checklist]
% [ ] Length/format: main text including figures, tables, and appendix within 10 pages, plus at most 2 pages for references only. Follow the ACM two-column template; over-length papers may be desk rejected.
% [ ] Anonymity: double-blind review. Remove author names, affiliations, and identifying information; write self-citations in third person.
% [ ] Submission system: submit electronically through EasyChair.
% [ ] Reproducibility: provide reviewer-accessible data/code when possible.

\begin{document}

\title{\method: Self-Supervised Money Laundering Detection via Topology Repair}
% \author{Seonkyu Lim}
% \email{{sklim}@kftc.or.kr}
% \affiliation{%
%   \institution{KFTC}
%   \city{Seoul}
%   \country{South Korea}
% }


\renewcommand{\shortauthors}{\method}


\begin{abstract}
In anti-money laundering (AML) transaction graphs, suspicious accounts have predominantly benign neighbors, so graph neural network (GNN) aggregation dilutes an already rare suspicious signal. We trace this to a topology-behavior mismatch: the manipulated transaction network fails to reflect homophily within the suspicious class. To address this mismatch, we present \method, a graph contrastive learning framework that performs topology repair through behavioral homophily recovery. \method builds a topology-independent recovered neighborhood graph from behavioral similarity, such as amount distributions. Contrastive learning then aligns the repaired topology with the transaction graph. On \hofinet, a retail inter-bank transfer network, the repair raises the suspicious ego-neighborhood ratio from 1:5.7 to 1:1.4. Across three AML datasets, \method outperforms supervised GNN and tabular baselines; subgraph-level pooling on the repaired topology adds further gains. At 1\% labels, \method achieves the highest suspicious-class F1 on all three datasets, with a 0.010--0.021 gap over the strongest supervised baseline. These findings position \method as a label-light building block for money laundering detection under realistic label-delay constraints.
\end{abstract}

\keywords{topology repair, behavioral homophily recovery, anti-money laundering, transaction networks, low-homophily, self-supervised learning, graph contrastive learning}

\maketitle

\section{Introduction}

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{figures/fig1_star_vs_recovered_cluster.pdf}
    \Description{Topology repair example from the actual HOFINET experiment data. A suspicious account has a benign-dominated transaction-star neighborhood, while the recovered behavioral-homophily graph recovers a suspicious peer cluster.}
  \caption{Topology repair on a suspicious account from the actual \hofinet experiment data, not a schematic. Left: the observed transaction graph forms a near-pure star with 26 one-hop counterparties, only 1 of them suspicious; the red dashed ring marks a benign-labeled transaction neighbor whose behavioral k-NN neighborhood is 8/10 suspicious under the repaired topology. Right: the recovered neighborhood graph constructed from the same experimental node features ($k{=}10$) recovers a compact peer cluster with 10 neighbors, 9 of them suspicious. Node colors use the ground-truth experimental labels: S = suspicious and B = benign; solid gray edges are observed transactions and dashed brown edges are behavioral-homophily k-NN links.}
\label{fig:intro_topology_repair}
\end{figure}

\begin{figure*}[t]
    \centering
    \includegraphics[width=0.96\textwidth]{figures/fig_multihop_correspondence.pdf}
    \Description{One-to-one correspondence between the recovered behavioral-homophily neighborhood and the original multi-hop transaction topology for the same suspicious account. Numbered nodes identify the same accounts across the two panels.}
  \caption{One-to-one correspondence between topology repair and the original multi-hop transaction topology for the same suspicious account from the actual \hofinet experiment data. Left: the union of shortest transaction paths from the ego account to the recovered accounts, together with the separate transaction component containing recovered nodes 1 and 4. Right: the recovered behavioral-homophily k-NN graph. Numbers identify the same accounts in both panels, and dotted gray connectors show the 1:1 correspondence. Eight of ten recovered nodes are reachable in the ego transaction component (one at 2 hops and seven at 4 hops), while recovered nodes 1 and 4 are connected by their own 4-hop transaction path in another component.}
\label{fig:multihop_correspondence}
\end{figure*}

Money laundering, accounting for an estimated 2--5\% of global GDP~\cite{unodc_ml}, has become more sophisticated through tactics such as structuring, layering, and mule networks. GNN-based methods have thus been explored to learn structural patterns in transaction networks beyond rule-based detection~\cite{cheng2025graph}. In AML, however, neighborhood aggregation is only useful when a node's neighbors carry class-relevant evidence. For the suspicious class, this evidence is often weak because suspicious accounts intentionally mix with benign counterparties. AML data are also highly imbalanced and labeled with substantial delays. We focus on three recurring challenges in AML transaction networks: suspicious-class neighborhood heterophily~\cite{zhu2020beyond}, severe class imbalance, and delayed or limited labels~\cite{fatf2013guidance}. To address these jointly, we adopt a topology-repair perspective on the underlying topology--behavior mismatch.


\textbf{Challenge 1: weak suspicious-class neighborhood signal.} Money-laundering accounts intentionally interact with benign accounts to evade detection, forming mule networks. In a synthetic transaction graph based on \hofinet, about 85\% of suspicious-incident edges connect to benign accounts (S-S:S-B $=$ 1:5.7). Many individual suspicious accounts are even more deeply embedded in benign neighborhoods (Figure~\ref{fig:intro_topology_repair}). As a result, expressive message passing struggles to accumulate suspicious evidence from local neighborhoods. Suspicious--suspicious links are scarce, despite high global homophily dominated by benign--benign edges. Standard graph contrastive learning (GCL)~\cite{you2020graph,velivckovic2018deep} cannot resolve this mismatch either. Typical graph augmentations perturb the transaction graph without altering which accounts are linked. Suspicious accounts therefore remain embedded among many benign neighbors across both contrastive branches~\cite{zhu2021graph}.

\textbf{Challenges 2--3: class imbalance and delayed labels.} Suspicious accounts often constitute less than 2\% of all accounts~\cite{hilal2022financial}. Labels arrive only after retrospective investigations that can take more than six months~\cite{fatf2013guidance,kofia2024aml}. Together with the weak suspicious-class neighborhood signal, these constraints create cold-start issues for supervised fraud models in production.

We propose \method, a graph contrastive learning framework that operationalizes topology repair through behavioral homophily recovery. Two linked questions guide our analysis. (i) When can behavioral homophily recovery repair the suspicious-class topology? (ii) How should aggregation operate on the repaired topology without diluting the suspicious signal? These questions form a single causal chain. Homophily recovery first determines whether class-relevant neighborhoods exist in the recovered topology (Theorems~\ref{thm:knn_homophily}--\ref{thm:combined}). On those neighborhoods, a sign-flip threshold governs whether pooling preserves or destroys the suspicious signal (Theorem~\ref{thm:meanpool}). Both mechanisms become observable only when normalization avoids collapse under the BYOL-style objective (Theorem~\ref{thm:bn_collapse}).

Our framing departs from prior k-NN auxiliary-graph constructions (e.g., MLGCL) in two ways. First, prior designs treat a feature-space affinity graph as a complement to the existing topology. They implicitly assume the input graph carries useful (if noisy) class structure, which is violated when the topology is adversarially manipulated. Second, we frame the affinity graph as a replacement: the recovered neighborhood graph replaces broken suspicious-class neighborhoods before contrastive learning. Two testable consequences follow. First, only behavioral features can repair the topology, since connectivity-derived ones inherit the adversarial structure. Second, aggregation should operate on the repaired topology, not the original transaction graph. We validate both in Section~\ref{sec:experiments} and support them theoretically in Appendix~\ref{app:theory}.

On \hofinet, the recovered neighborhood graph improves the S-S:S-B ratio from 1:5.7 to 1:1.4; AMLworld~\cite{amlpatterns2024realistic} and AMLNet exhibit the same pattern. Our contributions are summarized below.

\begin{itemize}
    \item \textbf{Topology repair via behavioral homophily recovery.} We build a topology-independent recovered neighborhood graph. The repair is characterized empirically (homophily, S-S:S-B ratio) and theoretically via a DCSBM-based bound (Appendix~\ref{app:theory}).

    % --- Deprecated contribution bullet (single 1% snapshot) ---
    % \item \textbf{From recovered homophily to usable representations under label delay.} \method acquires representations that remain competitive on three AML datasets with BYOL-style self-supervision, even in a cold-start scenario with under 1\% labeled data ($F1_{\text{susp}}{=}0.654$).
    % --- End deprecated ---
    \item \textbf{Self-supervised representations under label scarcity.} \method's frozen representations remain competitive with supervised tabular and GNN baselines in the cold-start regime, addressing the multi-month label delays typical of AML.

    \item \textbf{Signal preservation under aggregation.} A sign-flip threshold characterizes when pooling on the repaired topology preserves versus dilutes the suspicious signal; we validate the predicted regime empirically.
\end{itemize}


% \noindent\textbf{One-sentence framing.} The core contribution is topology repair via a behavioral homophily recovery graph, applied before contrastive learning.
\begin{figure*}[h]
\centering
\scalebox{0.79}{
\begin{tikzpicture}[
  scale=1.0, transform shape, >=Stealth,
  node distance=0.4cm and 0.6cm,
  inputbox/.style={
    draw=gray!70, fill=gray!10, rounded corners=4pt,
    minimum width=2.0cm, minimum height=1.2cm, align=center, thick
  },
  viewbox/.style={
    draw, rounded corners=4pt,
    minimum width=3.0cm, minimum height=1.2cm, align=center, thick
  },
  view1/.style={viewbox, draw=blue!60, fill=blue!8},
  view2/.style={viewbox, draw=orange!100, fill=orange!10},
  encbox/.style={
    draw=teal!70, fill=teal!7, rounded corners=4pt,
    minimum width=3.0cm, minimum height=1.2cm, align=center, thick
  },
  poolbox_oth/.style={
    draw=blue!30, fill=blue!3, rounded corners=4pt,
    minimum width=3.0cm, minimum height=1.2cm, align=center, thick
  },
  poolbox_our/.style={
    draw=orange!90, fill=orange!9, rounded corners=4pt,
    minimum width=3.0cm, minimum height=1.2cm, align=center, thick
  },
  projbox/.style={
    draw=purple!60, fill=purple!6, rounded corners=4pt,
    minimum width=2.5cm, minimum height=1.2cm, align=center, thick
  },
  lossbox/.style={
    draw=red!60, fill=red!6, rounded corners=4pt,
    minimum width=2.5cm, minimum height=1.5cm, align=center, thick
  },
  infbox/.style={
    draw=green!60!black, fill=green!6, rounded corners=4pt,
    minimum width=2.0cm, align=center, thick
  },
  secbox/.style={
    draw=black!30, dashed, rounded corners=6pt,
    inner sep=8pt, thick
  },
  myarrow/.style={->, thick, gray!70},
  emaarrow/.style={->, thick, purple!70, dashed},
]
% --- COLUMN 1: Input ---
\node[inputbox] (txgraph) {Transaction\\Graph $\mathcal{G}_{\text{trans}}$};
% --- COLUMN 2: Topology branches ---
\node[view1, right=1.0cm of txgraph, yshift=+1.4cm] (v1)
  {Observed topology\\{Augmented $\mathcal{G}_{\text{trans}}$}\\{(edge/feat.\ drop)}};
\node[view2, right=1.0cm of txgraph, yshift=-1.4cm] (v2)
  {{Recovered topology}\\{ $\mathcal{G}_{\text{bhv}}$ (top-$k$ sim.)}};
\draw[myarrow] (txgraph.east) -- ++(0.4,0) |- (v1.west);
\draw[myarrow] (txgraph.east) -- ++(0.4,0) |- (v2.west);
\node[above=0.2cm of v1, font=\bfseries, text=blue!70] (lv) {Topology Branches};
% --- COLUMN 3: Encoders ---
\node[encbox, right=0.5cm of v1] (enc1)
  {Online Encoder\\$f_\theta$ (shared)};
\node[encbox, right=0.5cm of v2] (enc2)
  {Target Encoder\\$f_\xi$ (EMA copy)};
\draw[myarrow] (v1.east) -- (enc1.west);
\draw[myarrow] (v2.east) -- (enc2.west);
\draw[emaarrow] (enc1.south) -- node[pos=0.5, right=2pt, text=purple!80]{EMA} (enc2.north -| enc1.south);
\node[above=0.2cm of enc1, font=\bfseries, text=teal!70] {GNN Encoder};
% --- COLUMN 4: Subgraph Pool ---
\node[poolbox_oth, right=0.5cm of enc1] (pool1)
  {Subgraph Pool\\{tx-graph neighbors}};
\node[poolbox_our, right=0.5cm of enc2] (pool2)
  {Subgraph Pool\\recovered neighbors};
\draw[myarrow] (enc1.east) -- (pool1.west);
\draw[myarrow] (enc2.east) -- (pool2.west);
\node[above=0.2cm of pool1, font=\bfseries, text=black] {Subgraph Pooling};
% --- COLUMN 5: Projectors ---
\node[projbox, right=0.5cm of pool1] (proj1)
  {Projector\\$g_\theta$};
\node[projbox, right=0.5cm of pool2] (proj2)
  {Target Projector\\$g_\xi$ (stop-grad.)};
\draw[myarrow] (pool1.east) -- (proj1.west);
\draw[myarrow] (pool2.east) -- (proj2.west);
\node[above=0.2cm of proj1, font=\bfseries, text=purple!70] {Projector};
% --- COLUMN 6: Loss + Inference ---
\node[lossbox, right=0.5cm of proj1, yshift=-1.4cm] (loss)
  {BYOL Loss $\mathcal{L}_B$\\{symmetric, no neg.}};
\draw[myarrow] (proj1.east) -| (loss.north);
\draw[myarrow] (proj2.east) -| (loss.south);
\node[infbox, right=0.5cm of loss] (inf)
  {Inference\\
    $\mathbf{z}_i=[\mathbf{s}_i^{(1)}\|\mathbf{s}_i^{(2)}]$
    $\downarrow$ {\scriptsize LogReg}\\
    {$F1_{\text{susp}}$/AUROC}};
\draw[myarrow] (loss.east) -- (inf.west);
\node[above=0.2cm of loss, yshift=0.8cm, font=\bfseries, text=red!60] {Contrastive Learning};
% --- Background boxes ---
\begin{scope}[on background layer]
  \node[secbox, fit=(v1)(v2), fill=blue!3, inner xsep=4pt, inner ysep=2pt] {};
  \node[secbox, fit=(enc1)(enc2), fill=teal!3, inner xsep=4pt, inner ysep=4pt] {};
  \node[secbox, fit=(pool1)(pool2), fill=gray!2, inner xsep=4pt, inner ysep=4pt] {};
  \node[secbox, fit=(proj1)(proj2), fill=purple!3, inner xsep=4pt, inner ysep=4pt] {};
  \node[secbox, fit=(loss)(inf), fill=red!2, inner xsep=4pt, inner ysep=4pt] {};
\end{scope}
% --- Ablation table ---
\node[below=1.0cm of loss, xshift=2.0cm,
  draw=black!30, rounded corners=3pt, inner sep=5pt,
  fill=white] (abltbl) {
  \begin{tabular}{@{}clcc@{}}
    \hline
    & \textbf{Setting} & \textbf{Topology} & \textbf{Level} \\
    \hline
    {\color{blue!60}$\blacksquare$} & (a) & Aug.\ $\mathcal{G}_{\text{trans}}$ & Node \\
    {\color{orange!80}$\blacksquare$} & (b) & Recovered & Node \\
    {\color{blue!30}$\blacksquare$} & (c) & Aug.\ $\mathcal{G}_{\text{trans}}$ & Subgraph \\
    {\color{orange!60}$\blacksquare$} & \textbf{(d)} & Recovered & Subgraph \\
    \hline
  \end{tabular}
};
\end{tikzpicture}
}% end resizebox
\caption{Overview of \method. We contrast the observed transaction topology with a topology-repaired recovered neighborhood graph constructed by behavioral homophily recovery. Both topology branches are encoded by a shared online GNN $f_\theta$ and its EMA target $f_\xi$, trained with a symmetric BYOL-style loss without negatives. The contrasted representation varies by setting: (a)(b) use node embeddings, while (c)(d) apply subgraph pooling before contrast. Bottom legend: four-setting ablation; (d) is the proposed topology-repair setup.}
\label{fig:framework}
\end{figure*}

\section{Related Work}\label{sec:related_work}
We review three threads of prior work that intersect with \method: heterophily-aware GNNs, graph contrastive learning with topology-repair graphs, and fraud or anomaly detection under label scarcity.

\subsection{Heterophily-aware GNNs}
A line of work improves message passing when label homophily is low by separating ego and neighbor embeddings, modifying propagation, or decoupling structure from attributes (e.g., H2GCN~\cite{zhu2020beyond}, Geom-GCN~\cite{pei2020geom}, LINKX~\cite{zhu2021linkx}). These models still operate on the observed transaction topology; in AML, suspicious accounts intentionally mix with benign counterparties, which can make suspicious--suspicious edges structurally scarce. \method complements heterophily-aware propagation by repairing the topology itself: it builds a class-relevant neighborhood structure from a topology-independent behavioral homophily recovery graph.

\subsection{GCL with Topology-Repair Graphs}
Self-supervised GCL learns representations by enforcing consistency across multiple graph branches~\cite{velivckovic2018deep,you2020graph}. Most existing approaches either perturb a single input graph or form branches at the model and representation level, leaving the underlying neighborhood structure essentially unchanged. Auxiliary-graph approaches introduce a separate graph, such as a k-NN affinity graph~\cite{shang2023mlgcl}; however, when built from all available features, this graph can entangle behavioral similarity with topology-correlated variables. \method constructs a topology-independent recovered neighborhood graph from behavioral features only and uses it for topology repair, then studies when aggregation on the repaired topology preserves versus dilutes suspicious evidence. Unlike prior k-NN auxiliary graphs that mainly enrich an existing topology, we motivate the construction as topology repair: when the observed graph is adversarially manipulated to be heterophilous (e.g., mule networks), the recovered neighborhood graph replaces broken neighborhood structure so downstream learning can operate on class-relevant neighborhoods.

\subsection{Money Laundering and Fraud Detection}
In AML and fraud detection, supervised GNNs can improve detection under class imbalance via neighborhood selection and reweighting~\cite{dou2020enhancing}. However, they typically require training-time labels that are delayed and limited in AML operations. Unsupervised anomaly detection provides label-free alternatives~\cite{ding2019dominant}, but is mostly evaluated on general graph benchmarks and does not directly address the suspicious-class neighborhood mismatch in transaction networks. \method targets this gap by combining topology-independent behavioral homophily recovery with self-supervised learning, and by analyzing aggregation-level signal preservation on the repaired topology.

\section{Method: \method}
\label{sec:method}

\method comprises a behavioral homophily recovery graph, a topology-repair contrastive encoder with subgraph pooling, and BYOL-style training.

\subsection{Problem Formulation}
We model the transaction network as a directed graph $\mathcal{G}_{\text{trans}} = (\mathcal{V}, \mathcal{E}, \mathbf{X})$, where $\mathcal{V}$ is the set of accounts (nodes, $|\mathcal{V}|=N$), $\mathcal{E} \subseteq \mathcal{V} \times \mathcal{V}$ is the set of transfer transactions (edges), and $\mathbf{X} \in \mathbb{R}^{N \times F}$ is the node feature matrix. Each node $v_i$ has a suspicion label $y_i \in \{0, 1\}$, but we use a self-supervised setting that ignores labels during training. Our goal is to learn node representations $\mathbf{Z} \in \mathbb{R}^{N \times D}$ that identify suspicious accounts based purely on the graph structure and node features.

\subsection{Feature Taxonomy}
\label{subsec:feature_taxonomy}
We group the node features available in a transaction network into behavioral, network-derived, and structural categories (Table~\ref{tab:features}).

The behavioral features capture transaction patterns via amount statistics, time-window frequencies, and transaction-type entropies (Table~\ref{tab:features}). Network-derived features (transaction counts and degrees) are input to the encoder but are excluded from the recovered-neighborhood construction, while structural features (centrality, hub/authority, topology) are used only for analysis and auxiliary comparison. We denote by $\mathbf{X}^{\text{enc}}$ the behavioral and network-derived features given to the GNN encoder, and by $\mathbf{X}^{\text{bhv}}$ the 20 behavioral features used to construct a behavioral homophily recovery graph. When suspicious accounts transact with benign ones to evade detection, the suspicious class exhibits low neighborhood homophily in the observed transaction topology. We counter this by recovering topology-independent behavioral homophily, linking accounts with similar behavior even if they are not directly connected by transactions.

\begin{table}[h]
\centering
\caption{Node feature taxonomy. Behavioral and network-derived features are used as GNN input, while recovered-neighborhood construction uses only 20 behavioral variables.}
\label{tab:features}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llrcc}
\toprule
\textbf{Category} & \textbf{Features} & \textbf{\#} & \textbf{GNN} & \textbf{Repair} \\
\midrule
\multicolumn{2}{l}{\textit{\textbf{Behavioral}}} & \textit{Total} - 20 & & \\
\midrule
\textbf{Amount stats}     & \makecell[l]{\text{in\_mean, in\_max, in\_std}\\\text{out\_mean, out\_max, out\_std}} & 6 & \checkmark & \checkmark \\
\textbf{Temporal (3min)}    & \makecell[l]{\text{in\_3m\_mean, in\_3m\_count}\\\text{out\_3m\_mean, out\_3m\_count}} & 4 & \checkmark & \checkmark \\
\textbf{Temporal (6min)}    & \makecell[l]{\text{in\_6m\_mean, in\_6m\_count}\\\text{out\_6m\_mean, out\_6m\_count}} & 4 & \checkmark & \checkmark \\
\textbf{Temporal (12min)}   & \makecell[l]{\text{in\_12m\_mean, in\_12m\_count}\\\text{out\_12m\_mean, out\_12m\_count}} & 4 & \checkmark & \checkmark \\
\textbf{Entropy}          & \makecell[l]{\text{md\_type\_entropy, fnd\_type\_entropy}} & 2 & \checkmark & \checkmark \\
\midrule
\multicolumn{2}{l}{\textit{Network-derived}} & \textit{Total} - 4 & & \\
\midrule
Count            & out\_count, in\_count            & 2 & \checkmark &  \\
Degree           & in\_dc, out\_dc                  & 2 & \checkmark &  \\
\midrule
\multicolumn{2}{l}{\textit{Structural}} & \textit{Total} - 7 & & \\
\midrule
Centrality       & dc, pagerank, betweenness        & 3 &  &  \\
Hub/Authority    & hits\_hub, hits\_auth             & 2 &  &  \\
Topology         & kcore, triangle                  & 2 &  &  \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Recovered Neighborhood Graph}
\label{subsec:knn_view}
\method realizes topology repair as a concrete graph object: a topology-independent recovered neighborhood graph $\mathcal{G}_{\text{bhv}}$ that replaces the heterophilous suspicious-class neighborhoods of the transaction topology. Standard augmentation-based GCL produces contrastive inputs by dropping edges or masking features on the original graph, so the underlying transaction neighborhoods are left intact. We instead build $\mathcal{G}_{\text{bhv}}$ independently of the edge structure of $\mathcal{G}_{\text{trans}}$ (Figure~\ref{fig:framework}) and use only behavioral features, so that the repair does not leak connectivity-derived information back into the construction.

\textbf{Recovered-neighborhood construction.} We first standardize the behavioral features $\mathbf{x}_i^{\text{bhv}} \in \mathbb{R}^{20}$ to have a zero mean and unit variance to remove scale differences across variables, and then apply L2 normalization, such that similarity depends on the direction of the feature vector:

\begin{equation}
\label{eq:l2norm}
    \bar{\mathbf{x}}_i = \text{StandardScaler}(\mathbf{x}_i^{\text{bhv}}), \quad
    \tilde{\mathbf{x}}_i = \frac{\bar{\mathbf{x}}_i}{\|\bar{\mathbf{x}}_i\|_2}
\end{equation}

Because the Euclidean distance between two L2-normalized vectors is monotonic in their cosine similarity, we retrieve the $k$ nearest neighbors of each node $v_i$ using a ball-tree search over Euclidean distance. Connecting each node to these neighbors yields the recovered neighborhood graph $\mathcal{G}_{\text{bhv}} = (\mathcal{V}, \mathcal{E}_{\text{bhv}})$:

\begin{equation}
\label{eq:knn_edge}
    \mathcal{E}_{\text{bhv}} = \{(v_i, v_j) \mid v_j \in \operatorname{kNN}(v_i, k)\}.
\end{equation}

We use the symmetrized (bidirectional) version of this recovered neighborhood graph for message passing and pooling; accordingly, we add $(v_j, v_i)$ whenever $(v_i, v_j) \in \mathcal{E}_{\text{bhv}}$.

Here, $\operatorname{kNN}(v_i, k)$ denotes the set of $k$ nodes closest (in Euclidean distance) to $\tilde{\mathbf{x}}_i$, with $k=10$ by default.

Money-laundering accounts that split transactions and layer transfers often show similar patterns in amount distributions, time-window frequencies, and transaction-type entropies. The recovered neighborhood graph thus links suspicious accounts that are not directly connected by transactions, recovering suspicious-to-suspicious neighborhoods missing in the transaction topology.

\subsection{Topology-Repair GNN Encoder}
\label{subsec:dual_view}
\textbf{Topology-repair contrastive branches.} Contrastive learning aligns the observed transaction topology, which captures transfer structure, with the recovered neighborhood topology, which captures behavioral homophily for the same account. \method forms these branches from $\mathcal{G}_{\text{trans}}$ and $\mathcal{G}_{\text{bhv}}$, with stochastic edge dropping and feature masking applied during training.

\textbf{Observed and repaired topologies.} Using only the transaction topology mixes suspicious and benign accounts, while using only the recovered neighborhood graph loses transaction-structure information. \method learns a shared account representation across both branches, so embeddings capture both transaction structure and behavioral homophily.

\textbf{GNN encoder.} We apply stochastic edge dropping and feature masking ($p_e{=}p_f{=}0.3$) to the transaction graph and recovered neighborhood graph to form two contrastive branches. A shared online encoder $f_\theta$ outputs embeddings $\mathbf{H}^{(1)} = f_\theta(\mathbf{X}^{\text{enc}}, \tilde{\mathcal{E}}_{\text{trans}})$ and $\mathbf{H}^{(2)} = f_\theta(\mathbf{X}^{\text{enc}}, \tilde{\mathcal{E}}_{\text{bhv}})$. The target encoder $f_\xi$ is an EMA copy that provides stop-gradient representations for the opposite BYOL branch. The online encoder stacks GCNConv~\cite{kipf2016semi} layers with batch normalization, nonlinear activation, and dropout. \method is encoder-agnostic and supports nine encoders, including GIN~\cite{xu2018powerful}, MVGRL, and GBT; Section~\ref{subsec:rq2} studies the effect of batch normalization.

\subsection{Subgraph Pooling}
\label{subsec:subgraph_pool}
\textbf{Subgraph-level signal aggregation.} \method compares not only individual node representations but also a subgraph representation that bundles each account with its neighbors (Subgraph Pooling in Figure~\ref{fig:framework}). This reflects the idea that whether an account is suspicious often becomes clearer from its relationships with neighboring counterparts than from its own attributes alone.

\textbf{Subgraph representation.} For each topology branch $t \in \{1,2\}$, we form the subgraph representation of node $v_i$ by averaging its own representation with the representations of its neighbors in that branch:
\begin{equation}\label{eq:subgraph_pool}
    \mathbf{s}_i^{(t)} = \frac{1}{|\mathcal{N}^{(t)}(v_i)| + 1}
    \left(\mathbf{h}_i^{(t)} + \sum_{v_j \in \mathcal{N}^{(t)}(v_i)} \mathbf{h}_j^{(t)}\right)
\end{equation}

Here $\mathcal{N}^{(1)}(v_i)$ is the neighborhood of $v_i$ in the transaction topology, and $\mathcal{N}^{(2)}(v_i)$ is its recovered neighborhood. For both topology branches, we perform message passing and pooling on a bidirected graph; hence, $\mathcal{N}^{(t)}(v_i)$ can be interpreted as the union of in- and out-neighbors. The mean pooling in Eq.~\eqref{eq:subgraph_pool} is the default in \method.

\textbf{Repair-aware pooling effect.} Subgraph pooling is conducted independently for each topology branch: transaction-graph neighbors are pooled for the observed topology, while behaviorally similar neighbors are pooled for the repaired topology. Because pooling aggregates neighbor information, the choice of topology shapes the outcome. The recovered neighborhood graph is designed to place behaviorally similar suspicious accounts closer together; therefore, pooling on the repaired topology can preserve or strengthen suspicious signals. In the transaction graph, suspicious accounts are often linked to benign ones (high S-B ratio); consequently, pooling can dilute suspicious signals with benign information.

\textbf{Pooling variants.} We use mean pooling by default. Other pooling choices are possible, including similarity-weighted pooling (HAP). We report its expected signal-preservation behavior in Appendix~\ref{app:theory} (Proposition~\ref{thm:hap}).

\subsection{Training and Inference}
\label{subsec:contrastive_learning}
\textbf{Projector and BYOL loss.} Subgraph-pooled representations pass through a projector $g$ and a predictor $q$ and are trained with a BYOL-style loss~\cite{grill2020bootstrap} (Projector and BYOL Loss in Figure~\ref{fig:framework}). Instead of pushing other accounts as negatives, the loss pulls together representations of the same account across the observed and repaired topology branches. Both branches are encoded by the online encoder $f_\theta$ and the target encoder $f_\xi$, and the loss sums the $1{\to}2$ and $2{\to}1$ directions. The target encoder is a momentum copy of the online encoder ($\xi \leftarrow m\xi + (1-m)\theta$, $m=0.99$), retaining most of the previous target while slowly incorporating new information.

We denote the loss input representation by $\mathbf{r}_i^{(t)}$. In our setting, $\mathbf{r}_i^{(t)}=\mathbf{s}_i^{(t)}$; in the node-level ablation, $\mathbf{r}_i^{(t)}=\mathbf{h}_i^{(t)}$. Let $\mathbf{r}_{\xi,i}^{(t)}$ be the corresponding target-branch representation, and $g_\xi$ the target-branch projector. The BYOL loss maximizes cosine similarity between representations from the two topology branches:
\begin{equation}\label{eq:loss}
    \mathcal{L} = \sum_{t \in \{1,2\}} \left(2 - 2 \cdot \frac{1}{N}\sum_{i=1}^{N} \frac{q(g(\mathbf{r}_i^{(t)})) \cdot g_\xi(\mathbf{r}_{\xi,i}^{(3-t)})}{\|q(g(\mathbf{r}_i^{(t)}))\| \cdot \|g_\xi(\mathbf{r}_{\xi,i}^{(3-t)})\|}\right)
\end{equation}

Here $g$ and $q$ are the projection and predictor heads, and $\mathbf{r}_{\xi,i}^{(\cdot)}$ is the input representation obtained from the target encoder branch.

\textbf{Inference.} At inference, we freeze the encoder and concatenate the observed-topology and repaired-topology representations to obtain the final embedding (right side of Figure~\ref{fig:framework}). In the subgraph-level setting, we concatenate subgraph representations:
\begin{equation}\label{eq:concat}
    \mathbf{z}_i = [\mathbf{s}_i^{(1)} \| \mathbf{s}_i^{(2)}].
\end{equation}
For node-level ablations, we use $\mathbf{z}_i = [\mathbf{h}_i^{(1)} \| \mathbf{h}_i^{(2)}]$.

Encoder architectures and training hyperparameters are available via an anonymized link for double-blind review.

\section{Experiments}
\label{sec:experiments}
We assess \method in four phases, moving from mechanism to practical deployment. First, we examine whether the recovered neighborhood graph can make up for missing suspicious-to-suspicious links and restore suspicious-class homophily, thereby boosting downstream performance. Second, we test when aggregating on the repaired topology (subgraph pooling) helps versus hurts, and how normalization affects training stability. Third, we benchmark the resulting self-supervised representations against supervised baselines under label delay and cold-start conditions, reflecting realistic AML settings. Fourth, we test whether the same mechanism and empirical patterns carry over to AML datasets that differ in prevalence and scale. Together, these experiments address the following research questions.

\begin{itemize}
\item \textbf{RQ1. Behavioral homophily recovery:} Does the topology-independent recovered neighborhood graph repair the suspicious-class topology relative to the transaction graph?
\item \textbf{RQ2. Signal preservation under aggregation:} When does subgraph pooling help versus hurt, and how does recovered homophily change this trade-off?
\item \textbf{RQ3. Self-supervised AML detection under label scarcity:} Does the self-supervised representation from \method remain competitive with supervised models under label delay and cold start?
\item \textbf{RQ4. Robustness across AML regimes:} Do the same mechanisms and trends apply to AML datasets with different prevalence and scale?
\end{itemize}

\subsection{Experimental Setting}
\textbf{Datasets.} We use three AML datasets (Table~\ref{tab:datasets}). \hofinet includes about 4.7M synthetic transactions derived from real activity on a national interbank settlement network, modeling 40 months of household and corporate transfers. AMLworld~\cite{amlpatterns2024realistic}, the NeurIPS 2023 open synthetic benchmark, models eight transaction patterns (e.g., fan-out, scatter-gather, cycle); we use the HI-Small variant (high illicit ratio, small size). AMLNet~\cite{huda2025amlnet}, a small synthetic benchmark for the high-prevalence regime, shares AMLworld’s feature schema.

\textbf{Account-level labels.} All three datasets provide transaction-level suspicious labels. Following standard graph-based AML and fraud detection practices~\cite{dou2020enhancing,liu2021pick}, we max-aggregate to the account level: an account is suspicious (label$=1$) if it appears on either side of any suspicious transaction, and normal (label$=0$) otherwise. The resulting suspicious-account ratios are 2.13\% (\hofinet), 1.23\% (AMLworld), and 13.52\% (AMLNet) (Table~\ref{tab:datasets}).

\begin{table}[ht]
\centering
\caption{Dataset statistics. $\rho$ = suspicious account ratio.}
\label{tab:datasets}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lrrrl}
\toprule
\textbf{Dataset} & $|V|$ & $|E|$ & $\rho$ & \textbf{Type} \\
\midrule
\hofinet           & 452{,}816 & 4{,}732{,}130 & 2.13\% & Synthetic (real-derived) \\
AMLworld          & 515{,}088 & 5{,}078{,}345 & 1.23\% & Synthetic (simulator) \\
AMLNet            &  11{,}000 & 1{,}090{,}172 & 13.52\% & Synthetic (simulator) \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Ablation settings and encoders.} We evaluate four settings that cross topology branch and contrastive level: (a) transaction graph + node contrast, (b) recovered neighborhood graph + node contrast, (c) transaction graph + subgraph pooling, and (d) repaired topology + subgraph pooling. These settings are tested with nine encoders spanning per-layer BN (GBT, DGI+BN, MVGRL+BN, GRACE+BN, GIN), final-only BN (BGRL), and BN-less variants (DGI, MVGRL, GRACE). All encoders use the same BYOL-style bootstrap loss (Eq.~\eqref{eq:loss}) and hyperparameters (lr=5e-4, hidden=256, $L$=2, dropout=0.2, 200 epochs); Appendix~\ref{app:loss_table} verifies that this substitution does not penalize the baselines.

% --- Deprecated baseline list (11 models for Table 6 single-snapshot comparison) ---
% \textbf{Supervised baselines.} We apply class-weight correction to 11 supervised models: generic GNN (GCN~\cite{kipf2016semi}, GAT~\cite{velivckovic2017graph}, GraphSAGE~\cite{hamilton2017inductive}), tabular methods (MLP, LightGBM~\cite{lightgbm2017}, XGBoost~\cite{xgboost2016}), and fraud- and anomaly-specific GNNs (CARE-GNN~\cite{dou2020enhancing}, PC-GNN~\cite{liu2021pick}, BWGNN~\cite{tang2022rethinking}, GAGA~\cite{wang2023label}, ConsisGAD~\cite{chen2024consisgad}). Since our datasets are homogeneous single-relation graphs of account-to-account transfers, we evaluate the single-relation versions of the fraud-specific GNN baselines (see Section~\ref{subsec:rq3}).
% --- End deprecated ---
\textbf{Supervised baselines.} For the label-efficiency comparison (Section~\ref{subsec:rq3}), we evaluate six supervised baselines spanning three families: tabular (MLP, XGBoost~\cite{xgboost2016}, LightGBM~\cite{lightgbm2017}), generic GNN (GCN~\cite{kipf2016semi}, GAT~\cite{velivckovic2017graph}), and fraud-specific GNN (CARE-GNN~\cite{dou2020enhancing}). MLP follows the standard convention common in fraud-GNN literature (stacked Linear$+$ReLU; no BatchNorm). All baselines use class-weight correction and use the same encoder-input features $\mathbf{X}^{\text{enc}}$ as \method (20 behavioral $+$ 4 network-derived; Table~\ref{tab:features}). Only the topology-repair graph construction uses behavioral features $\mathbf{X}^{\text{bhv}}$ (20) and excludes network-derived variables.

\textbf{Evaluation protocol.} The primary metric is suspicious-class F1 ($F1_{\text{susp}}$); AUROC and AUPRC are secondary. We use a 10\%/10\%/80\% train/val/test split (encoders are trained self-supervised; labels only train the Logistic Regression head; therefore, 10\% of labeled data suffices). For paired comparisons, the split is fixed per seed. We report mean$\pm$std over four seeds (2024--2027). Statistical significance uses a 4-seed paired $t$-test with Holm--Bonferroni correction; seed-level raw results and reproduction scripts are also available at this anonymized link.

\textbf{Evaluation environment.} We evaluate self-supervised models by freezing the encoder and training Logistic Regression~\cite{pedregosa2011scikit} on embeddings $\mathbf{z}_i = [\mathbf{s}_i^{(1)} \| \mathbf{s}_i^{(2)}]$. All experiments use Python 3.12, PyTorch 2.10, CUDA 12.8, and NVIDIA H100 80GB GPUs.

\subsection{Homophily Recovery (RQ1)}\label{subsec:rq1}
RQ1 tests whether the topology-independent recovered neighborhood graph repairs the suspicious-class topology relative to the transaction graph.

\textbf{Quantitative gains across all 9 encoders.} Replacing the transaction topology with the recovered neighborhood graph consistently outperforms the baseline across all nine encoders (Table~\ref{tab:rq1}), with gains of +84\% to +205\% for the six encoders with BN and +23\% to +28\% for the three without BN. For the six BN-equipped encoders, a four-seed paired $t$-test remains significant after Holm--Bonferroni correction (adjusted $p < 4 \times 10^{-5}$).

\begin{table}[h]
\centering
\caption{$F1_{\text{susp}}$ on \hofinet for nine encoders across the four topology-by-level ablation settings (4-seed mean$\pm$std). \textit{Paired BN}: three GCN encoder families (DGI, MVGRL, GRACE) with and without per-layer BN. \textit{Native BN}: reference encoders (GBT, BGRL, GIN). The \textbf{Native loss} column lists each encoder's loss in its original paper; every encoder here is trained with the unified BYOL-style bootstrap loss (Eq.~\eqref{eq:loss}) to isolate the topology-repair effect. Appendix~\ref{app:loss_table} verifies the substitution does not penalize the baselines. Best per row in \textbf{bold}, second-best \underline{underlined}.}
\label{tab:rq1}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lllcccccc}
\toprule
\textbf{Category} & \textbf{Encoder} & \textbf{BN} & \BLUE{\makecell{\textbf{Native loss}\\\textbf{($\mathcal{L}^{\star}=0$)}}} & \makecell{\textbf{Tx graph}\\\textbf{node}} & \makecell{\textbf{Recovered}\\\textbf{node}} & \makecell{\textbf{Tx graph}\\\textbf{pool}} & \makecell{\textbf{Repaired}\\\textbf{pool}} \\
\midrule
\multirow{6}{*}{\makecell[l]{Paired\\BN}}
  & DGI       & \xmark & \BLUE{JSD BCE}        & 0.043{\scriptsize±.001} & \underline{0.054}{\scriptsize±.006} & 0.048{\scriptsize±.004} & \textbf{0.068}{\scriptsize±.004} \\
  & DGI+BN    & \cmark & \BLUE{JSD BCE}        & 0.266{\scriptsize±.010} & \underline{0.670}{\scriptsize±.004} & 0.198{\scriptsize±.002} & \textbf{0.673}{\scriptsize±.002} \\
\cmidrule(l){2-8}
  & MVGRL     & \xmark & \BLUE{JSD multi-view} & 0.043{\scriptsize±.001} & \underline{0.055}{\scriptsize±.007} & 0.048{\scriptsize±.003} & \textbf{0.072}{\scriptsize±.008} \\
  & MVGRL+BN  & \cmark & \BLUE{JSD multi-view} & 0.266{\scriptsize±.010} & \underline{0.671}{\scriptsize±.004} & 0.198{\scriptsize±.002} & \textbf{0.673}{\scriptsize±.003} \\
\cmidrule(l){2-8}
  & GRACE     & \xmark & \BLUE{InfoNCE NT-Xent} & 0.044{\scriptsize±.001} & \underline{0.054}{\scriptsize±.005} & 0.045{\scriptsize±.003} & \textbf{0.070}{\scriptsize±.008} \\
  & GRACE+BN  & \cmark & \BLUE{InfoNCE NT-Xent} & 0.286{\scriptsize±.004} & \underline{0.670}{\scriptsize±.004} & 0.216{\scriptsize±.002} & \textbf{0.673}{\scriptsize±.003} \\
\midrule
\multirow{3}{*}{\makecell[l]{Native\\BN}}
  & GBT  & \cmark & \BLUE{Barlow Twins}   & 0.266{\scriptsize±.010} & \underline{0.671}{\scriptsize±.003} & 0.197{\scriptsize±.002} & \textbf{0.673}{\scriptsize±.003} \\
  & BGRL & \cmark & \BLUE{BYOL bootstrap} & 0.306{\scriptsize±.012} & \underline{0.561}{\scriptsize±.008} & 0.253{\scriptsize±.016} & \textbf{0.639}{\scriptsize±.005} \\
  & GIN  & \cmark & \BLUE{supervised CE}  & 0.181{\scriptsize±.035} & \underline{0.555}{\scriptsize±.038} & 0.127{\scriptsize±.001} & \textbf{0.578}{\scriptsize±.021} \\
\bottomrule
\end{tabular}}
\end{table}

\begin{table}[h]
\centering
\caption{S-S:S-B edge ratio and homophily by graph type across the three AML datasets. Best per dataset in bold, second-best underlined (higher homophily and a smaller S-B multiplier in 1:$x$ are better). The behavioral homophily recovery graph repairs the suspicious-class topology, while the structural-feature recovery ablation (\hofinet only) does not.}
\label{tab:ss_sb}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{llcc}
\toprule
\textbf{Dataset} & \textbf{Graph} & \textbf{Homophily} & \textbf{S-S:S-B} \\
\midrule
\multirow{3}{*}{\hofinet}
  & Transaction      & 0.690 & \underline{1:5.7} \\
  & Structural repair & \underline{0.965} & 1:9.7 \\
  & Behavioral repair & \textbf{0.981} & \textbf{1:1.4} \\
\midrule
\multirow{2}{*}{AMLworld}
  & Transaction      & \underline{0.886} & \underline{1:15.8} \\
  & Behavioral repair & \textbf{0.980} & \textbf{1:9.4} \\
\midrule
\multirow{2}{*}{AMLNet}
  & Transaction      & \underline{0.791} & \underline{1:7.2} \\
  & Behavioral repair & \textbf{0.900} & \textbf{1:1.3} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Topology repair via behavioral similarity.} The transaction graph's S-S:S-B ratio of 1:5.7 (Table~\ref{tab:ss_sb}) improves to 1:1.4 under the recovered neighborhood graph (Figure~\ref{fig:intro_topology_repair}), and edge homophily $h$ (the fraction of edges whose endpoints share the same label) increases from 0.690 to 0.981. Because money-laundering accounts share transaction patterns such as split transfers and multi-hop routing, behavioral similarity tends to reconnect them in the recovered graph. The structural-feature recovery ablation, in contrast, worsens the ratio to 1:9.7, which supports our design choice to exclude structural features from the construction and to use them only for analysis.

\textbf{From the case study to Table~\ref{tab:rq1}.} For the Figure~\ref{fig:intro_topology_repair} account, the encoder's aggregation neighborhood is $3.8\%$ suspicious in (a), $7.4\%$ in (c), $90.0\%$ in (b), and $90.9\%$ in (d). The $\le 7.4\%$ signals in (a)/(c) push the account into the benign region (false negative, contributing to the GBT scores $0.266$ and $0.197$), while the $\ge 90\%$ signals in (b)/(d) classify it correctly ($0.671$ and $0.673$); pooling (c$\to$d) sharpens this gap because the suspicious ego reinforces a suspicious-peer cluster but not a benign-dominated star. The single-account flip generalizes: across all $9{,}644$ suspicious accounts (Figure~\ref{fig:rq1_shift_hist}), $47.7\%\to 71.6\%$ have a majority-suspicious 1-hop ego after repair, and $66.1\%$ of the $5{,}044$ majority-benign-ego accounts flip to majority-S. Four further checks rule out a \hofinet-specific cause: (i) feature-specific: structural-feature k-NN worsens the ratio to 1:9.7 (Table~\ref{tab:ss_sb}); (ii) encoder-agnostic: all 9 encoders show the same direction of (a)$\to$(b) gain; (iii) cross-dataset: AMLworld and AMLNet replicate the gain (Tables~\ref{tab:ss_sb},~\ref{tab:rq4}); (iv) theoretically aligned: the measured homophily gap lies within $0.019$ of the Theorem~\ref{thm:combined} bound.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\columnwidth]{figures/fig_rq1_shift_hist.pdf}
\caption{Population-level S-ratio shift across all 9{,}644 suspicious \hofinet accounts. Topology repair shifts the bimodal transaction-graph distribution (gray) past the majority-suspicious threshold (dashed): $47.7\%\to 71.6\%$ majority-S 1-hop egos.}
\label{fig:rq1_shift_hist}
\end{figure}

\textbf{Robustness to $k$.} On GBT, $k=10$ is optimal over $k \in \{5, 10, 20, 50\}$, and $k=5$ remains strong at (d) $F1_{\text{susp}} = 0.676$. For $k>10$, benign mixing reduces S-S density, and (d) decreases monotonically ($k{=}50$: 0.661). The full-range spread of (d) $F1_{\text{susp}}$ is only 0.021, indicating robustness to $k$.

\textbf{Additional checks.} Seed-level analyses show that constructing the recovered neighborhood graph from behavioral features alone outperforms an all-feature baseline (analogous to MLGCL- and GCPAL-style affinity-graph construction) by $+0.040$ $F1_{\text{susp}}$ on \hofinet. The repair is a one-time preprocessing step (608.9\,s on \hofinet). We report an additional pooling variant (HAP) in Appendix~\ref{app:theory}.

\subsection{Signal Preservation (RQ2)}
\label{subsec:rq2}
RQ1 verified topology repair in the recovered neighborhood graph; RQ2 asks when aggregation (subgraph pooling) helps versus hurts, and how the repaired topology changes this trade-off. Pooling is not uniformly beneficial on the raw transaction graph; however, on the repaired topology, it can amplify suspicious evidence, with the clearest gains under suspicious-class-oriented downstream scoring.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\columnwidth]{figures/fig_rq2_ablation.pdf}
\caption{$F1_{\text{susp}}$ over four ablation settings of the two design axes (topology branch $\times$ contrastive level) on \hofinet, averaged over 4 per-layer BN encoders (GBT, DGI+BN, MVGRL+BN, GRACE+BN). Settings: (a) transaction topology+node, (b) recovered neighborhood+node, (c) transaction topology+pool, (d) repaired topology+pool (proposed).}
    \label{fig:rq2_ablation}
\end{figure}

\textbf{Pooling effects across the 4-setting ablation.} The effect of subgraph pooling depends on both topology homophily and the encoder's normalization depth (Figure~\ref{fig:rq2_ablation}). On \hofinet, the (d)-over-(b) gain shrinks as normalization deepens: four per-layer BN encoders gain only +0.002--0.003, BGRL gains +0.078, and BN-less encoders gain +0.018 on average. Setting (c), pooling on the transaction graph, reduces performance by 17--30\% relative to (a) for BN-equipped encoders ($-17.1\%$ for BGRL to $-29.9\%$ for GIN; (c) bars in Figure~\ref{fig:rq2_ablation}), consistent with the sign-flip analysis in Appendix~\ref{app:theory} (Theorem~\ref{thm:meanpool}).

On \hofinet, topology repair produces the dominant jump, with (b)$>$(a) and (d)$>$(c); pooling then adds a smaller, normalization-dependent increment, with (d)$\approx$(b) for per-layer BN and larger gains for BGRL and BN-less encoders. The (b)$\to$(d) improvement is significant under paired $t$-tests for representative encoders (GBT $p=1.83{\times}10^{-3}$, BGRL Holm-adjusted $p=2.66{\times}10^{-4}$). On AMLworld, Table~\ref{tab:rq4} shows that aggregation on the repaired topology (d) improves over node-level evaluation on the same graph (b), suggesting that the recovered neighborhoods provide additional class context that pooling can accumulate.

Overall, the effect of subgraph pooling depends on (i) recovered homophily and (ii) encoder normalization depth.

\begin{table*}[h]
\centering
\caption{BatchNorm architectural ablation summary at setting (d) on \hofinet (4-seed paired $t$-test, Holm-Bonferroni adjusted; \# = pairs within family).}
\label{tab:bn_summary}
\begin{tabular}{llccc}
\toprule
\textbf{Axis} & \textbf{Better $>$ Worse (mean $F1_{\text{susp}}$)} & $\boldsymbol{\Delta F1_{\text{susp}}}$ & \textbf{\# pairs} & $\boldsymbol{\max p_{\text{adj}}}$ \\
\midrule
(i) BN presence  & \{DGI, MVGRL, GRACE\}+BN (0.673) $>$ no-BN (0.070)            & 0.602 & 3 & $1.21\times 10^{-6}$ \\
(ii) BN position & Per-layer 4 enc.\ (0.673) $>$ Final-only BGRL (0.639)          & 0.034 & 4 & $5.55\times 10^{-3}$ \\
(iii) Conv type  & GCNConv 4 enc.\ (0.673) $>$ GINConv GIN (0.578)                & 0.095 & 4 & $1.25\times 10^{-2}$ \\
\bottomrule
\end{tabular}
\end{table*}

\textbf{Normalization as a stability factor.} We use Theorem~\ref{thm:bn_collapse} together with the homophily-recovery and signal-preservation analyses in Appendix~\ref{app:theory} as a unified theoretical lens. Under the BYOL-style bootstrap loss, normalization affects training stability (Figure~\ref{fig:rq2_bn}, settings (a) vs. (d) across nine encoders). Table~\ref{tab:bn_summary} reports 4-seed paired $t$-tests with Holm correction for setting (d). (i) The presence of BN yields the largest performance gap ($\Delta F1_{\text{susp}} \approx 0.60$) in controlled comparisons that differ only in BN, while (ii) the position of BN (per-layer $>$ final-only) and (iii) the type of convolution (GCNConv $>$ GINConv) show smaller but significant effects. Across paradigms, per-layer BN encoders converge to 0.673--0.674 under the bootstrap loss and stay within 0.006 under their paper-native losses (Table~\ref{tab:loss_table}), suggesting that once training stabilizes, the repaired topology is the major driver of performance.

\begin{figure}[h]
    \centering
    \begin{subfigure}{0.49\columnwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/fig_rq3_bn_a.pdf}
        \caption{BN-augmented encoders}
        \label{fig:rq2_bn_a}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.49\columnwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/fig_rq3_bn_b.pdf}
        \caption{BN-free encoders}
        \label{fig:rq2_bn_b}
    \end{subfigure}
    \caption{$F1_{\text{susp}}$ trajectories for four ablations (a)/(b)/(c)/(d) across 9 encoders on \hofinet (4-seed mean$\pm$std). Note the differing y-axis scales between panels.}
    \label{fig:rq2_bn}
\end{figure}

\begin{theorem}[Normalization-conditional stability under bootstrap objectives]\label{thm:bn_collapse}
Under the BYOL-style bootstrap loss~\eqref{eq:loss}, the trivial constant solution $f_\theta(\cdot) \equiv c$ attains $\mathcal{L}=0$ when the encoder lacks batch normalization (BN). In contrast, the presence of at least one BN layer rules out this trivial constant mapping under the definition of batch normalization, acting as a sufficient condition to avoid the constant-solution pathology.
\end{theorem}

\textbf{Empirical check for the stability analysis.} At setting (a), BN-less encoders achieve $F1_{\text{susp}} \approx 0.04$, close to the \hofinet random-classifier upper bound $2\rho/(1+\rho) \approx 0.042$ (the maximum $F1_{\text{susp}}$ at a positive rate $\rho$), indicating that unstable training can wash out the suspicious-class signal in low-prevalence regimes.
The practical severity depends on prevalence: at low prevalence ($\rho \leq 2\%$), the bound is already $\approx 0.04$; hence, instability leaves little usable suspicious-class evidence. On AMLNet ($\rho = 13.52\%$), BN-less DGI still reaches $F1_{\text{susp}} \approx 0.40$ (Section~\ref{subsec:rq4}); consequently, the absolute impact is smaller. The BN-conditional pattern extends beyond the BYOL bootstrap loss: bare DGI, MVGRL, and GRACE under their paper-native JSD and InfoNCE losses also collapse to $F1_{\text{susp}} \approx 0.07$ (Table~\ref{tab:loss_table}), so the mechanism in Theorem~\ref{thm:bn_collapse} is BN-conditional rather than loss-specific. Overall, the results align with a single mechanism: homophily recovery provides class-relevant neighborhoods, sign-flip explains when aggregation can dilute them, and normalization mainly modulates whether this mechanism is realized.

\subsection{Label-Efficiency (RQ3)}
\label{subsec:rq3}

RQ3 asks whether \method's self-supervised representation remains competitive with supervised baselines as labels become scarce. We vary the train label fraction in $\{1\%, 5\%, 10\%\}$ and compare \method against six supervised baselines spanning three families: tabular (MLP, XGBoost~\cite{xgboost2016}, LightGBM~\cite{lightgbm2017}), generic GNN (GCN~\cite{kipf2016semi}, GAT~\cite{velivckovic2017graph}), and fraud-specific GNN (CARE-GNN~\cite{dou2020enhancing}). MLP follows the standard convention (stacked Linear$+$ReLU; no BatchNorm) common in fraud-GNN literature. All models use the same encoder-input features $\mathbf{X}^{\text{enc}}$ (20 behavioral $+$ 4 network-derived; Table~\ref{tab:features}); \method uses setting (d) with the strongest encoder per dataset (GBT on \hofinet and AMLNet, BGRL on AMLworld).

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig_rq3_labeff.pdf}
\caption{Label-efficiency comparison on three AML datasets across train label fractions $\{1\%, 5\%, 10\%\}$ (4-seed mean$\pm$std, shaded bands).}
\label{fig:rq3_labeff}
\end{figure*}

\textbf{\method dominates the 1\%-label cold-start regime.} At 1\% labels, \method attains the highest $F1_{\text{susp}}$ on every dataset: 0.654 on \hofinet, 0.062 on AMLworld, and 0.632 on AMLNet. The margin over the next-best supervised baseline is $+0.021$ (\hofinet, vs.\ XGBoost), $+0.017$ (AMLworld, vs.\ MLP), and $+0.010$ (AMLNet, vs.\ MLP). Among graph baselines, \method exceeds the strongest (GAT) by $+0.25$ on \hofinet, $+0.02$ on AMLworld, and $+0.26$ on AMLNet at 1\% labels.

\textbf{Tabular boosting catches up at 10\% labels.} XGBoost and LightGBM approach \method monotonically as labels increase. On AMLworld, XGBoost reaches $F1_{\text{susp}}=0.073$ at 10\% labels versus \method's 0.066; on AMLNet, XGBoost reaches 0.699 versus \method's 0.679. The gap to \method shrinks as the label budget grows, supporting the operational claim that \method's main value lies in the label-delayed regime characteristic of AML operations~\cite{hilal2022financial}.

\textbf{BN-conditional collapse generalizes from self-supervised to supervised models.} On the low-prevalence \hofinet ($\rho{=}2.13\%$) and AMLworld ($\rho{=}1.23\%$), bare MLP collapses to near-random performance ($F1_{\text{susp}}\approx 0.05$, close to the random-classifier upper bound $2\rho/(1+\rho)$). The same model reaches $F1_{\text{susp}}=0.62$--$0.68$ on AMLNet ($\rho{=}13.52\%$), where the random-classifier ceiling is much higher ($\approx 0.24$). This mirrors the BN-free self-supervised encoders analyzed in Theorem~\ref{thm:bn_collapse}: collapse-prevention—via BatchNorm or sufficient prevalence—becomes critical in low-prevalence regimes, regardless of whether training is supervised or self-supervised. \method's encoder uses per-layer BatchNorm and avoids this collapse on all three datasets.

\textbf{Graph baselines remain consistently weaker than \method.} GCN, GAT, and CARE-GNN trail \method by $0.13$--$0.57$ $F1_{\text{susp}}$ at 10\% labels on \hofinet, $0.02$ on AMLworld, and $0.16$--$0.24$ on AMLNet. These baselines aggregate on the original transaction topology and inherit its anti-homophilous neighborhood structure; \method first repairs that topology before contrastive training. The gap is stable across all label fractions, indicating that topology repair, not message-passing capacity, is the binding constraint for graph-based AML detection.

\textbf{Operational utility with a limited analyst budget.} \method with GBT attains \hofinet precision@$k$ of 0.875, 0.853, 0.834, 0.791, and 0.746 for $k \in \{50, 100, 200, 400, 800\}$ (4 seeds), detecting approximately 317 true suspicious accounts within a top-400 budget.

\begin{table}[h]
\centering
\caption{RQ4: $F1_{\text{susp}}$ on AMLworld ($\rho{=}1.23\%$) and AMLNet ($\rho{=}13.52\%$) across the four topology-by-level settings (4-seed mean$\pm$std).}
\label{tab:rq4}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
 & & \multicolumn{2}{c}{\textbf{Node-level}} & \multicolumn{3}{c}{\textbf{Subgraph Pooling}} \\
\cmidrule(lr){3-4} \cmidrule(lr){5-7}
\textbf{Encoder} & \textbf{BN} & \textbf{(a) Tx} & \textbf{(b) Repair} & \textbf{(c) Tx} & \textbf{(d) Repair}  \\
\midrule
\multicolumn{7}{l}{\textit{AMLworld (N=515{,}088, $\rho$=1.23\%)}} \\\midrule
BGRL      & \checkmark & 0.038{\scriptsize±.003} & \underline{0.061}{\scriptsize±.003} & 0.039{\scriptsize±.002} & 0.066{\scriptsize±.008} \\
GBT       & \checkmark & 0.042{\scriptsize±.002} & \underline{0.049}{\scriptsize±.001} & 0.043{\scriptsize±.001} & 0.064{\scriptsize±.004} \\
DGI+BN    & \checkmark & 0.042{\scriptsize±.002} & \underline{0.049}{\scriptsize±.001} & 0.044{\scriptsize±.001} & 0.062{\scriptsize±.005} \\
GRACE+BN  & \checkmark & 0.041{\scriptsize±.001} & \underline{0.048}{\scriptsize±.001} & 0.044{\scriptsize±.000} & 0.061{\scriptsize±.004} \\
% DGI       &            & 0.038{\scriptsize±.007} & \underline{0.049}{\scriptsize±.004} & 0.033{\scriptsize±.005} & \textbf{TBD{\scriptsize±TBD}} \\
\midrule
\multicolumn{7}{l}{\textit{AMLNet (N=11{,}000, $\rho$=13.52\%)}} \\
\midrule
BGRL      & \checkmark & 0.448{\scriptsize±.018} & \underline{0.668}{\scriptsize±.013} & 0.295{\scriptsize±.013} & 0.680{\scriptsize±.008} \\
GBT       & \checkmark & 0.426{\scriptsize±.011} & \underline{0.669}{\scriptsize±.012} & 0.302{\scriptsize±.009} & 0.688{\scriptsize±.009} \\
DGI+BN    & \checkmark & 0.426{\scriptsize±.011} & \underline{0.672}{\scriptsize±.014} & 0.302{\scriptsize±.010} & 0.688{\scriptsize±.008} \\
GRACE+BN  & \checkmark & 0.431{\scriptsize±.014} & \underline{0.673}{\scriptsize±.010} & 0.299{\scriptsize±.006} & 0.685{\scriptsize±.009} \\
% DGI       &            & 0.397{\scriptsize±.015} & \underline{0.593}{\scriptsize±.014} & 0.301{\scriptsize±.007} & \textbf{TBD{\scriptsize±TBD}} \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Robustness across AML Regimes (RQ4)}\label{subsec:rq4}

Finally, RQ4 asks whether the patterns established in RQ1–RQ3 transfer to AML regimes with different prevalence and scale. We therefore repeat the same ablation on two structurally distinct datasets: AMLworld, a large low-prevalence simulator dataset ($\rho{=}1.23\%$), and AMLNet, a smaller high-prevalence benchmark ($\rho{=}13.52\%$). Table~\ref{tab:rq4} reports $F1_{\text{susp}}$ across topology-by-level settings for five encoders.

The cross-regime trend is consistent: topology repair provides most of the gains, and subgraph-level evaluation sharpens suspicious-class decisions once the topology has been repaired.

\textbf{AMLworld: amplification in the low-prevalence regime.} On AMLworld, BGRL reaches $F1_{\text{susp}}=0.066$ in (d), a 73\% gain over (a), and all encoders satisfy (b) $>$ (a). In this low-prevalence regime ($\rho{=}1.23\%$), subgraph pooling on the repaired topology (d) beats node-level evaluation on the same graph (b): once topology repair restores neighborhood class context, subgraph pooling aggregates it into stronger suspicious-account evidence, including for per-layer BN encoders.

\textbf{AMLNet: high prevalence reduces instability in BN-free encoders.} On AMLNet, the baseline (a) already attains $F1_{\text{susp}}=0.40$--$0.45$, far above \hofinet (0.04--0.27) and AMLworld (0.038--0.042). The high suspicious-account ratio (13.52\%) keeps even the BN-free DGI encoder outside the unstable regime described in Theorem~\ref{thm:bn_collapse}. Collapse prevention is most critical at low prevalence and weakens as the random-classifier ceiling rises. Topology-repair and contrastive-level effects still match \hofinet; for example, GBT rises from 0.426 in (a) to 0.679 in (d), a relative gain of over 50\%. All three datasets show the same topology-repair pattern reported in Table~\ref{tab:ss_sb}.

\section{Conclusion and Future Work}
We introduced \method, a graph contrastive learning framework that performs topology repair for AML detection in low-homophily transaction networks. \method anchors its contrastive objective to a behavioral homophily recovery graph that replaces the broken suspicious-class neighborhoods of the transaction topology, while retaining the transaction branch at inference (Eq.~\ref{eq:concat}) so that downstream fund-flow tracing can still operate on real transactions. This repair raises \hofinet edge homophily from 0.690 to 0.981. Under realistic label scarcity (1\% labels), \method achieves the highest $F1_{\text{susp}}$ on all three datasets (e.g., 0.654 on \hofinet), exceeding the strongest supervised baseline by 0.010--0.021. At 10\% labels, tabular boosting models catch up on AMLworld and AMLNet, while \method retains the lead on \hofinet and surpasses all supervised graph baselines across the board.

The results support one mechanism: topology repair yields class-relevant neighborhoods; sign-flip analysis shows when aggregation can dilute them; subgraph pooling on the repaired topology refines them conditionally; and normalization mainly stabilizes the bootstrap objective. These claims follow from the stability analysis (Theorem~\ref{thm:bn_collapse}) and the homophily-recovery and aggregation-signal analyses (Theorems~\ref{thm:knn_homophily}--\ref{thm:meanpool}), with detailed statements in Appendix~\ref{app:theory}.

\method targets AML regimes with delayed labels, low prevalence, and anti-homophilous suspicious neighborhoods. In high-prevalence regimes with abundant labels, stronger supervised baselines exist; \method is designed for the label-delayed, low-prevalence regime that characterizes operational AML. 

Limitations remain. Our evaluation is transductive and does not include temporal hold-out splits; all three benchmarks are synthetic. Real-world labeled AML transaction graphs are typically protected by banking secrecy and confidential reporting frameworks, which restrict redistribution even for anonymized snapshots. Future work includes evaluation with temporal splits and validation on real transaction data when possible.

\textbf{Detection vs.\ investigation.} \method optimizes detection accuracy: the recovered neighborhood graph drives the gains in Table~\ref{tab:rq1}, and the transaction graph is retained at inference but is not specifically optimized for the investigation workflow. Suspicious Transaction Report filing, mule-network reconstruction, and fund-flow tracing still operate on the original transaction topology; the recovered graph cannot stand in for it post-detection. We position \method as the first-stage screen in a two-stage pipeline (behavioral screening followed by transaction-graph investigation), not as a wholesale replacement for the transaction graph.

\textbf{Reproducibility.} We will release the source code, training scripts, and seed-level raw results. \hofinet cannot be redistributed due to its data-sharing agreement, but the AMLworld and AMLNet experiments are reproducible from public benchmarks.


% 임시 주석처리
% \bibliographystyle{ACM-Reference-Format}
% \bibliography{aml}

\appendix
\section{Datasets: Brief Description}\label{app:datasets}

\subsection{\hofinet}
\label{app:dataset_hofinet}
\hofinet is a synthetic transaction dataset based on a national inter-bank transfer system. The network includes both household (internet and mobile banking) and corporate firm-banking channels. Our snapshot has 452,816 accounts and 4,732,130 directed multi-edges, including 9,644 suspicious accounts (2.13\%). The simulator generates six AML typologies, including structuring, layering, and mule networks, over 40 months from 2021Q3 to 2024Q4.

\subsection{AMLworld}
\label{app:dataset_amlworld}
AMLworld~\cite{amlpatterns2024realistic} is a public synthetic AML benchmark from the NeurIPS 2023 Datasets \& Benchmarks track. We use the HI-Small configuration with 515,088 accounts and 5,078,345 directed multi-edges. The laundering rate at the transaction level is 0.10\%, and the fraction of suspicious accounts is 1.23\% (lower than \hofinet’s 2.13\%). The simulator includes eight AML patterns: fan-out, fan-in, scatter-gather, gather-scatter, cycle, bipartite, stack, and random.

\subsection{AMLNet}
\label{app:dataset_amlnet}
AMLNet~\cite{huda2025amlnet} is a smaller synthetic AML benchmark with 11,000 accounts and 1,090,172 directed multi-edges. The fraction of suspicious accounts is 13.52\% (1,487 accounts), which is much higher than in \hofinet and AMLworld; therefore, it is useful for high-prevalence evaluation. AMLNet uses the same 20 behavioral features as AMLworld, which enables cross-dataset transfer analysis in Appendix~\ref{app:transfer}.


\subsection{Cross-Dataset Transfer}\label{app:transfer}

To test whether the self-supervised representations of \method transfer across domains, we use an encoder pre-trained on AMLworld and evaluate it on AMLNet (Table~\ref{tab:transfer}). The datasets use the same 20 behavioral features, but they differ in prevalence (1.23\% vs. 13.52\%) and graph size (515K vs. 11K nodes). Linear probing performs poorly, likely due to this distribution shift, while fine-tuning recovers and slightly exceeds from-scratch training.

\begin{table}[H]
\centering
\caption{Cross-dataset transfer from AMLworld (source, 200-epoch self-supervised pretraining) to AMLNet (target). \method (d) GBT encoder, 4-seed mean$\pm$std on the 80\% holdout. Fine-tune mode continues training on the target for 50 epochs.}
\label{tab:transfer}
\small
\begin{tabular}{lccc}
\toprule
\textbf{Mode} & \textbf{$F1_{\text{susp}}$} & \textbf{AUROC} & \textbf{AUPRC} \\
\midrule
From-scratch (Table~\ref{tab:rq4}) & 0.679{\scriptsize±.002} & 0.928{\scriptsize±.001} & 0.751{\scriptsize±.005} \\
Linear probe (frozen)              & 0.218{\scriptsize±.011} & 0.510{\scriptsize±.016} & 0.139{\scriptsize±.002} \\
\textbf{Fine-tune (50 ep)}         & \textbf{0.690}{\scriptsize±.006} & \textbf{0.934}{\scriptsize±.003} & \textbf{0.784}{\scriptsize±.004} \\
\bottomrule
\end{tabular}
\end{table}


\section{Label-Efficiency: Full Numerical Results}
\label{app:label_eff_full}

Table~\ref{tab:label_eff_full} reports the per-cell numbers underlying Figure~\ref{fig:rq3_labeff} ($F1_{\text{susp}}$ at train label fractions $\{1\%, 5\%, 10\%\}$ on three datasets, 4-seed mean$\pm$std). All baselines share the 20 behavioral features (Table~\ref{tab:features}); the boosting baselines (XGBoost, LightGBM) drop the structural feature group for the same fair comparison.

\begin{table*}[t]
\centering
\caption{Full numerical results for Figure~\ref{fig:rq3_labeff}: $F1_{\text{susp}}$ (4-seed mean$\pm$std) across train label fractions $\{1\%, 5\%, 10\%\}$ on three AML datasets. Best per (dataset, fraction) in \textbf{bold}, second-best \underline{underlined}. \method uses setting (d) with the strongest encoder per dataset (GBT on \hofinet and AMLNet, BGRL on AMLworld); MLP follows the standard fraud-GNN convention (Linear$+$ReLU, no BatchNorm).}
\label{tab:label_eff_full}
\resizebox{\textwidth}{!}{%
\begin{tabular}{l ccc ccc ccc}
\toprule
& \multicolumn{3}{c}{\textbf{\hofinet ($\rho{=}2.13\%$)}} & \multicolumn{3}{c}{\textbf{AMLworld ($\rho{=}1.23\%$)}} & \multicolumn{3}{c}{\textbf{AMLNet ($\rho{=}13.52\%$)}} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7} \cmidrule(lr){8-10}
\textbf{Model} & \textbf{1\%} & \textbf{5\%} & \textbf{10\%} & \textbf{1\%} & \textbf{5\%} & \textbf{10\%} & \textbf{1\%} & \textbf{5\%} & \textbf{10\%} \\
\midrule
\method & \textbf{0.654}{\scriptsize±.009} & \textbf{0.671}{\scriptsize±.001} & \textbf{0.673}{\scriptsize±.003} & \textbf{0.062}{\scriptsize±.014} & 0.064{\scriptsize±.012} & 0.066{\scriptsize±.009} & \textbf{0.632}{\scriptsize±.080} & 0.673{\scriptsize±.012} & 0.679{\scriptsize±.001} \\
MLP & 0.052{\scriptsize±.011} & 0.059{\scriptsize±.004} & 0.059{\scriptsize±.016} & 0.045{\scriptsize±.002} & 0.040{\scriptsize±.010} & 0.046{\scriptsize±.002} & \underline{0.622}{\scriptsize±.085} & \textbf{0.685}{\scriptsize±.010} & 0.683{\scriptsize±.005} \\
XGBoost & \underline{0.633}{\scriptsize±.015} & \underline{0.661}{\scriptsize±.004} & \underline{0.672}{\scriptsize±.002} & 0.022{\scriptsize±.003} & \textbf{0.070}{\scriptsize±.002} & \textbf{0.073}{\scriptsize±.002} & 0.503{\scriptsize±.099} & \underline{0.677}{\scriptsize±.021} & \textbf{0.699}{\scriptsize±.007} \\
LightGBM & 0.616{\scriptsize±.010} & 0.657{\scriptsize±.006} & 0.669{\scriptsize±.002} & 0.022{\scriptsize±.008} & \underline{0.068}{\scriptsize±.002} & \underline{0.070}{\scriptsize±.002} & 0.425{\scriptsize±.103} & 0.652{\scriptsize±.035} & \underline{0.687}{\scriptsize±.011} \\
GAT & 0.408{\scriptsize±.071} & 0.498{\scriptsize±.089} & 0.543{\scriptsize±.052} & \underline{0.046}{\scriptsize±.002} & 0.047{\scriptsize±.001} & 0.047{\scriptsize±.002} & 0.369{\scriptsize±.045} & 0.488{\scriptsize±.025} & 0.518{\scriptsize±.024} \\
GCN & 0.282{\scriptsize±.017} & 0.260{\scriptsize±.009} & 0.251{\scriptsize±.009} & 0.045{\scriptsize±.003} & 0.043{\scriptsize±.001} & 0.045{\scriptsize±.001} & 0.384{\scriptsize±.080} & 0.457{\scriptsize±.012} & 0.464{\scriptsize±.009} \\
CARE-GNN & 0.117{\scriptsize±.012} & 0.110{\scriptsize±.007} & 0.108{\scriptsize±.007} & 0.045{\scriptsize±.001} & 0.042{\scriptsize±.000} & 0.041{\scriptsize±.000} & 0.327{\scriptsize±.075} & 0.417{\scriptsize±.021} & 0.440{\scriptsize±.012} \\
\bottomrule
\end{tabular}}
\end{table*}


\section{Loss Substitution: Native vs.\ Unified BYOL Bootstrap}
\label{app:loss_table}

Every encoder in Tables~\ref{tab:rq1} and \ref{tab:rq4} uses the unified BYOL-style bootstrap loss (Eq.~\eqref{eq:loss}) to isolate the topology-by-level factor from the loss formulation. Table~\ref{tab:loss_table} verifies the substitution on \hofinet, setting (d), 4 seeds. BN-equipped baselines under their paper-native losses match the BYOL bootstrap result within $\pm 0.001$. Bare DGI, MVGRL, and GRACE collapse under both their native JSD/InfoNCE and BYOL bootstrap, indicating that the collapse pathology of Theorem~\ref{thm:bn_collapse} is BN-conditional rather than loss-specific.

\begin{table*}[h]
\centering
\caption{Loss substitution check on \hofinet, setting (d), 4-seed mean$\pm$std. \textbf{BYOL}: unified BYOL bootstrap loss; values are independent re-runs under the current code, paired with the Native column to remove run-to-run drift. \textbf{Native}: each encoder trained with its paper-native loss on the same (d) framework. $\Delta = \text{Native} - \text{BYOL}$. GIN's original paper uses supervised cross-entropy and has no contrastive native loss, so the Native column is omitted.}
\label{tab:loss_table}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llcccc}
\toprule
\textbf{Encoder} & \textbf{Native loss} & \textbf{BN} & \textbf{BYOL} $F1_{\text{susp}}$ & \textbf{Native} $F1_{\text{susp}}$ & $\Delta$ \\
\midrule
DGI~\cite{velivckovic2018deep}         & JSD BCE                & \xmark & 0.066{\scriptsize±.008} & 0.074{\scriptsize±.010} & $+0.008$ \\
DGI+BN~\cite{velivckovic2018deep}      & JSD BCE                & \cmark & 0.667{\scriptsize±.002} & 0.668{\scriptsize±.002} & $+0.001$ \\
MVGRL~\cite{hassani2020contrastive}    & JSD multi-view         & \xmark & 0.065{\scriptsize±.005} & 0.074{\scriptsize±.004} & $+0.009$ \\
MVGRL+BN~\cite{hassani2020contrastive} & JSD multi-view         & \cmark & 0.667{\scriptsize±.001} & 0.668{\scriptsize±.001} & $+0.001$ \\
GRACE~\cite{zhu2020deep}               & InfoNCE NT-Xent        & \xmark & 0.066{\scriptsize±.008} & 0.071{\scriptsize±.010} & $+0.005$ \\
GRACE+BN~\cite{zhu2020deep}            & InfoNCE NT-Xent        & \cmark & 0.670{\scriptsize±.003} & 0.670{\scriptsize±.002} & $\phantom{+}0.000$ \\
GBT~\cite{bielak2022graph}             & Barlow Twins           & \cmark & 0.667{\scriptsize±.002} & 0.667{\scriptsize±.003} & $\phantom{+}0.000$ \\
BGRL~\cite{thakoor2021large}           & BYOL bootstrap (same)  & \cmark & 0.628{\scriptsize±.001} & 0.627{\scriptsize±.003} & $-0.001$ \\
GIN~\cite{xu2018powerful}              & supervised CE          & \cmark & 0.585{\scriptsize±.027} & ---                     & ---      \\
\bottomrule
\end{tabular}}
\end{table*}

\textbf{BEHAViEW loss robustness.} On setting (d) with the GBT encoder, the four contrastive losses yield $F1_{\text{susp}}=0.667, 0.667, 0.668, 0.668$ (BootstrapLatent, BarlowTwins, InfoNCE, JSD; 4 seeds), confirming that the (d) framework, not the loss formulation, drives BEHAViEW's performance.

\textbf{Low loss at setting (a) is structural, not BYOL-specific.} Setting (a)'s two branches are stochastic perturbations of the same transaction graph, so an encoder can drive the loss down by learning a near-identity mapping that aligns near-identical inputs. Theorem~\ref{thm:bn_collapse} rules out the strictly constant solution that BN-less encoders fall into, but it does not prevent low-information solutions when the two branches are too similar to require discriminative encoding. Table~\ref{tab:loss_ab_check} shows that this account is loss-agnostic: on DGI+BN, all four contrastive losses produce a substantially lower final loss at (a) than at (b), while $F1_{\text{susp}}$ stays low at (a) ($\approx 0.26$) and recovers at (b) ($\approx 0.66$). The gap between (a) and (b) is therefore not a matter of training convergence quality but of what the encoder is forced to encode to drive the loss down.

\begin{table}[h]
\centering
\caption{DGI+BN on \hofinet across four contrastive losses, 4-seed mean. Setting (a): two perturbations of the transaction graph. Setting (b): transaction branch vs.\ recovered-neighborhood branch. Lower final loss at (a) with low $F1_{\text{susp}}$ indicates near-identity alignment, not Theorem~\ref{thm:bn_collapse}'s constant-solution pathology.}
\label{tab:loss_ab_check}
\begin{tabular}{lcccc}
\toprule
\textbf{Loss} & \textbf{(a) $\mathcal{L}_{\text{final}}$} & \textbf{(a) $F1_{\text{susp}}$} & \textbf{(b) $\mathcal{L}_{\text{final}}$} & \textbf{(b) $F1_{\text{susp}}$} \\
\midrule
BootstrapLatent & $\phantom{-}0.05$  & 0.266 & $\phantom{-1}0.45$ & 0.664 \\
Barlow Twins    & $\phantom{-}2.86$  & 0.270 & $\phantom{-}11.89$ & 0.664 \\
InfoNCE         & $\phantom{-}6.74$  & 0.263 & $\phantom{-1}7.55$ & 0.664 \\
JSD             & $-0.43$            & 0.255 & $\phantom{-1}{-0.20}$ & 0.666 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Cross-dataset consistency.} The substitution holds beyond \hofinet. On AMLworld ($\rho{=}1.23\%$), the three BN-equipped baselines under native losses reach $F1_{\text{susp}}=0.048$ (DGI+BN, JSD), $0.049$ (MVGRL+BN, JSD), and $0.054$ (GRACE+BN, InfoNCE); on AMLNet ($\rho{=}13.52\%$), they reach $0.678, 0.678, 0.674$ respectively. These match the BYOL bootstrap results in Table~\ref{tab:rq4} for the same encoders within $\pm 0.005$, confirming that the loss substitution does not penalize baselines on either the low- or high-prevalence regime.


\section{Theory: Proofs and Empirical Checks}
\label{app:theory}

This appendix presents the assumptions and main derivations used in the theoretical part of the paper, and it also reports simple numerical checks.

\subsection{Setup and Notation}
\label{app:theory_notation}

Let $V = V_S \cup V_B$ be the node set, where $V_S$ are suspicious ($S$) nodes and $V_B$ are benign ($B$) nodes. Let $N:=|V|$, $N_S:=|V_S|$, and $N_B:=|V_B|$. Define the prevalence $\rho:=N_S/N$ and $N_{\min}:=\min(N_S,N_B)$. For a graph $G=(V,E)$, define edge homophily as $h(G):=|\{(u,v)\in E: y_u=y_v\}|/|E|$.

Each node $v$ has behavioral features $x_v \in \mathbb{R}^d$. We assume i.i.d. class-conditional features: $x_v \mid y_v = c \sim P_c$ for $c \in \{S,B\}$. For any $x \in \text{supp}(P_y)$, $y' \neq y$, and independent samples $X' \sim P_y$, $X'' \sim P_{y'}$, we assume
\begin{equation}\label{eq:sep}
    \Pr\big[\|x - X'\| < \|x - X''\|\big] \;\geq\; 1 - \epsilon.
\end{equation}
That is, a same-class node is closer than a different-class node with probability at least $1-\epsilon$. When $\epsilon=0$, this becomes deterministic. Let $G_{\text{knn},k}$ be the Euclidean k-NN graph where each node connects to its $k$ nearest neighbors.

\subsection{Recovered Homophily Analysis}
\label{app:theory_view}

\begin{theorem}[Probabilistic homophily recovery]
\label{thm:knn_homophily}
Under $k \leq (1-\epsilon)(N_{\min}-1) - 1$, the k-NN graph $G_{\text{knn},k}$ constructed from behavioral features satisfies
\[
    h(G_{\text{knn}, k}) \;\geq\; 1 - \tfrac{N - 1}{k} \exp\!\left(-\tfrac{((N_{\min}-1)(1-\epsilon) - k)^2}{2(N_{\min}-1)(1-\epsilon)}\right).
\]
\end{theorem}
\noindent\textbf{Remark.} The condition $k \leq (1-\epsilon)(N_{\min}-1) - 1$ is mild in our setting: it requires $k$ to be much smaller than the minority-class size. In our datasets, $N_{\min}$ ranges from 1{,}487 (AMLNet) to 9{,}644 (\hofinet), and we use $k=10$ throughout.

For a suspicious node $u$, a benign node $B_j$ enters the top-$k$ neighbor list only if
$Z_j := |\{i : \|x_u - x_{S_i}\| < \|x_u - x_{B_j}\|\}| < k$.
By Assumption~\eqref{eq:sep}, $Z_j$ stochastically dominates $\text{Binom}(N_S{-}1,1{-}\epsilon)$. A Chernoff lower-tail bound plus a union bound gives Theorem~\ref{thm:knn_homophily}; in the deterministic limit $\epsilon=0$, the bound becomes $h(G_{\text{knn},k})=1$.

For the transaction graph $G_{\text{tx}}$, we use a degree-corrected stochastic block model (DCSBM)~\cite{karrer2011stochastic}. Each node $v$ has an activity parameter $\theta_v>0$, and $\omega_{y_u,y_v}$ controls the connection strength between classes. We assume
\begin{equation}\label{eq:dcsbm}
    \Pr[(u, v) \in E_{\text{tx}}] \;\propto\; \theta_u \theta_v \cdot \omega_{y_u, y_v}.
\end{equation}
Let $\Theta_c := \sum_{v: y_v=c}\theta_v$. Then
\[
    h(G_{\text{tx}}) = \frac{\Theta_S^2 \omega_{SS} + \Theta_B^2 \omega_{BB}}{\Theta_S^2 \omega_{SS} + 2\Theta_S\Theta_B \omega_{SB} + \Theta_B^2 \omega_{BB}}.
\]

To estimate the (node-averaged) ego-heterophily under the DCSBM, we sample a node $v$ uniformly at random from $V$, compute the expected cross-class neighbor fraction conditional on $y_v\in\{S,B\}$, and then average over the classes:
\begin{equation}
\label{eq:eta_dcsbm}
\eta_h^{(\text{dcsbm})}
\;=\;
\rho\,\frac{\Theta_B\omega_{SB}}{\Theta_S\omega_{SS}+\Theta_B\omega_{SB}}
\;+
(1-\rho)\,\frac{\Theta_S\omega_{SB}}{\Theta_B\omega_{BB}+\Theta_S\omega_{SB}}.
\end{equation}

\begin{theorem}[Combined homophily gap]
\label{thm:combined}
Under the DCSBM, if $h(G_{\text{tx}}) = \alpha$, then
\[
h(G_{\text{knn}, k}) - h(G_{\text{tx}}) \geq 1 - \alpha - \delta(N, N_{\min}, k, \epsilon),
\]
where $\delta$ is the exponential term in Theorem~\ref{thm:knn_homophily}.
\end{theorem}

Using Theorem~\ref{thm:knn_homophily} (i.e., $h(G_{\text{knn},k}) \geq 1-\delta$) and subtracting $h(G_{\text{tx}})=\alpha$, we obtain the homophily-gap bound:
\begin{equation}\label{eq:gap}
    h(G_{\text{knn}, k}) - h(G_{\text{tx}}) \;\geq\; 1 - \alpha - \tfrac{N - 1}{k} \exp\!\left(-\tfrac{((N_{\min}-1)(1-\epsilon) - k)^2}{2(N_{\min}-1)(1-\epsilon)}\right).
\end{equation}
On \hofinet ($N=452{,}816$, $N_{\min}=9{,}644$, $k=10$, $\alpha=0.690$) with $\epsilon=0.05$, the exponential term is negligible (below $10^{-1000}$), giving a bound of $\approx 0.310$. The measured gap is $0.981-0.690=0.291$, which is 0.019 below the bound. We treat this as a sufficient condition, not an exact predictor.

\subsection{Aggregation-level Signal Preservation}
\label{subsec:theory_pool}

We now study the aggregation step. We write the hidden representation of node $v$ as $z_v = s_v \mu + \eta_v$. Here $s_v \in \{\pm 1\}$ is the class signal, $\mu$ is the direction that separates suspicious and benign nodes, and $\eta_v$ is a bounded noise term with $\|\eta_v\| \leq \sigma \|\mu\|$. (We use $\sigma$ here to avoid confusion with the separability parameter $\epsilon$ in Assumption~\eqref{eq:sep}.) We define the ego-neighborhood heterophily as
$\eta_h := |\{u \in \mathcal{N}(v) : y_u \neq y_v\}| / |\mathcal{N}(v)|$.
We compare the post-pooling projection on $\mu$,
$\varrho := \langle z_v^{(\cdot)}, \mu \rangle / \|\mu\|^2$,
for mean pooling and for heterophily-aware pooling (HAP) with cosine-similarity weights. Without loss of generality, we set $s_v=+1$.

\begin{theorem}[Mean pool signal flip]
\label{thm:meanpool}
Assuming $s_v=+1$ without loss of generality, $\mathbb{E}[\varrho^{(\text{mean})}] = (1 + k(1 - 2\eta_h)) / (k+1)$. The mean-pooled signal flips sign (i.e., $\mathbb{E}[\varrho] < 0$) when $\eta_h > 1/2 + 1/(2k)$.
\end{theorem}

\begin{proposition}[HAP signal preservation under small-noise regime]
\label{thm:hap}
Assume $\sigma < 1/\sqrt{2}$ and that cosine similarities maintain the correct sign. Then, for any $\eta_h$, the HAP post-pooling expectation satisfies
\[
\mathbb{E}[\varrho^{(\text{hap})}] \geq 1 - 2\sigma^2 - 4\sigma^2 \eta_h.
\]
\end{proposition}

\paragraph{Predicted 4-setting ablation pattern.}\label{cor:ablation}
Assume an encoder with BN and $\eta_h^{\text{tx}} > 1/2 + 1/(2k)$. Then the predicted ranking of $F1_{\text{susp}}$ is $F1(b) \approx F1(d) > F1(a) > F1(c)$. On \hofinet, all six BN-equipped encoders follow this order ($\Delta(b{-}a) \in [+0.255, +0.405]$, $\Delta(c{-}a) \in [-0.070, -0.052]$).

\begin{table}[h]
\centering
\caption{Empirical comparison with the homophily-gap bound and the mean-pool flip condition on three AML datasets. \hofinet and AMLworld measured gaps are within $\sim 0.02$ of the idealized bound; AMLNet shows a larger gap of 0.10. Ego-heterophily exceeds the mean-pool flip threshold $\eta_h^* = 0.55$ on all three.}
\label{tab:theory_verify}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrrrrrr}
\toprule
\multirow{2}{*}{Dataset} & \multicolumn{4}{c}{T1: homophily gap (Theorem~\ref{thm:combined})} & \multicolumn{3}{c}{T2: ego-heterophily (Eq.~\eqref{eq:eta_dcsbm})} \\
\cmidrule(lr){2-5}\cmidrule(l){6-8}
 & $h_{\text{tx}}$ & $h_{\text{knn}}$ & gap & T1 bnd & $\eta_h$ meas & DCSBM pred & $\eta_h > \eta_h^*$ \\
\midrule
\hofinet  & 0.690 & 0.981 & 0.291 & $\geq$0.310 & 0.851 & 0.741 & \cmark \\
AMLworld & 0.886 & 0.980 & 0.094 & $\geq$0.114 & 0.941 & 0.888 & \cmark \\
AMLNet   & 0.791 & 0.900 & 0.109 & $\geq$0.209 & 0.878 & 0.783 & \cmark \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Cross-Dataset Empirical Check}\label{app:theory_verify}

Table~\ref{tab:theory_verify} compares the theory with the measured values. The homophily-gap lower bound in Theorem~\ref{thm:combined} is close to the measured gap on \hofinet and AMLworld (difference $\approx 0.02$ for $\epsilon=0.05$), but the gap is less tight on AMLNet, likely because AMLNet is smaller and has higher prevalence. For $k=10$, all datasets satisfy the mean-pool flip condition $\eta_h > 0.55$, and the DCSBM prediction in Eq.~\eqref{eq:eta_dcsbm} is 5--11\% lower than the measured $\eta_h$.


\clearpage
\bibliographystyle{ACM-Reference-Format}
\bibliography{aml}

\end{document}
