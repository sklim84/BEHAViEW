% \documentclass[sigconf,nonacm]{acmart}
\documentclass[sigconf,review]{acmart}

% \usepackage{multirow}
\usepackage{multirow, multicol, caption}
\captionsetup[table]{skip=2pt}
\usepackage{adjustbox}
\usepackage[ruled,noend]{algorithm2e} 
\DontPrintSemicolon
\usepackage{algorithmic}
\usepackage{bm}
\usepackage[utf8]{inputenc}
\usepackage{kotex}
\usepackage[textsize=small]{todonotes}
\usepackage{subfigure}
\usepackage{booktabs}
\usepackage{lipsum}
\usepackage[switch]{lineno}
\usepackage{bm}
\usepackage{adjustbox}
\usepackage{subfigure}
\urlstyle{same}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{multicol}
% \usepackage{graphicx}
\usepackage{siunitx}
\usepackage{rotating}
\usepackage{colortbl}
\usepackage{enumitem}
% \usepackage{pgfplots}
\usepackage{makecell}
\usepackage{amsmath}
\usepackage{tablefootnote}
\usepackage{graphicx}
\usepackage{placeins}
\usepackage{xurl}   % 또는 \usepackage{url}
\newcommand{\model}[1]{\texttt{\small\nolinkurl{#1}}}
%%%%%%%%%%%%%%%%%%%%   CUSTOM COMMAND    %%%%%%%%%%%%%%%%%%%%%
% \newcommand{\TODO}{\textcolor{red}{TODO}}
% \newcommand{\FIXME}{\textcolor{red}{FIXME}}
\newcommand{\RED}[1]{\textcolor{black}{#1}}
\newcommand{\BLUE}[1]{\textcolor{black}{#1}}
\newcommand{\kt}[1]{\textcolor{black}{#1}}
% \newcommand{\MG}[1]{\textcolor{magenta}{#1}}
% \newenvironment{thisnote}{\par\color{blue}}{\par}
% \usepackage[textsize=small]{todonotes}
% \usepackage{marginnote}
%  \let\marginpar\marginnote
% \setlength{\marginparwidth}{1.5cm}
% \newcommand{\insertref}[1]{\todo[color=green!40]{#1}}
% \newcommand{\explainindetail}[1]{\todo[color=red!40]{#1}}
% \newcommand{\insertcomment}[1]{\todo[]{#1}}
% \newcommand{\insertdone}[1]{\todo[fancyline,color=red!20!white]{#1}}
% \newcommand{\inserttag}[1]{\todo[fancyline,color=red!20!white]{#1}}
% \newcommand{\inserttodo}[1]{\todo[inline, inlinewidth=6cm]{#1}}
% \newcommand{\fancytodo}[1]{\todo[fancyline]{#1}}
\newcommand{\method}{BECON\xspace}
\usepackage{pifont}
\newcommand{\cmark}{\ding{51}} % check mark
\newcommand{\xmark}{\ding{55}} % x mark
%%%%%%%%%%%%%%%%%%%%   CUSTOM COMMAND    %%%%%%%%%%%%%%%%%%%%%

\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

\settopmatter{printacmref=false}
% \renewcommand{\footnotetextcopyrightpermission}[1]{}
% \pagestyle{plain}

%% Conference information
% \acmConference[KDD'26]{ACM SIGKDD Conference on Knowledge Discovery and Data Mining}{August 9--13, 2026}{Jeju, Republic of Korea}
% \acmYear{2026}
% \copyrightyear{2026}

\begin{document}

% 한글 제목: BECON: 저동질성 거래 네트워크에서의 자금세탁 탐지를 위한
%           행동 서브그래프 대조학습
% BECON = BEhavioral CONtrast
\title{BECON: Behavioral Subgraph Contrast for Anti-Money Laundering in Low-Homophily Transaction Networks}

% \author{Seonkyu Lim}
% \email{{sklim}@kftc.or.kr}
% \affiliation{%
%   \institution{KFTC}
%   \city{Seoul}
%   \country{South Korea}
% }


\renewcommand{\shortauthors}{\method}


\begin{abstract}
자금세탁 탐지는 금융 시스템의 건전성을 위해 필수적이나, 거래 네트워크의 저동질성과 극심한 클래스 불균형으로 인해 기존 그래프 신경망 기반 접근법은 한계를 보인다.
라벨 부족 환경에서 유망한 대안으로 부상하고 있는 그래프 대조학습 역시 기존 모델은 동일 그래프에 대한 확률적 증강으로 contrastive view를 생성하여 거래 네트워크 고유의 특성을 충분히 활용하지 못한다.
이러한 한계를 완화하기 위해, 본 연구에서는 \texttt{BECON}을 제안한다. \texttt{BECON}은 행동적 유사도에 기반한 k-NN 그래프를 보조 view로 구축하고, 서브그래프 수준의 이웃 집계를 적용하는 자금세탁 탐지 프레임워크이다. 
\texttt{BECON}은 그래프 대조학습의 성능을 결정하는 두 독립 축, 즉 view 구성과 contrastive level을 체계적으로 분석하여 최적 조합을 도출한다. 
실제 은행간 이체 데이터와 합성 벤치마크에서의 실험을 통해, 행동적 k-NN view가 의심 계좌 간 연결 비율을 크게 개선하여 동질성을 효과적으로 복원함을 밝힌다. 
또한 서브그래프 풀링은 행동적 view와 결합할 때만 효과적이며, BatchNorm이 대규모 불균형 환경에서 인코더 아키텍처와 무관하게 성능을 결정하는 핵심 요인임을 발견한다. 
나아가, \texttt{BECON}은 라벨 없이 학습한 자기지도 표현만으로 다수의 지도학습 모델과 동등한 탐지 성능을 달성하여, 자금세탁 탐지 도메인에서의 실용적 가치를 입증한다.
\end{abstract}

\keywords{anti-money laundering, graph contrastive learning, k-NN graph view, subgraph pooling, low-homophily, transaction networks}

\maketitle

\section{Introduction}

\begin{figure}[t!]
    \centering
    \includegraphics[width=0.85\columnwidth]{figures/fig_intro_homophily.pdf}
    \caption{Suspicious-to-Suspicious (S-S) 대 Suspicious-to-Benign (S-B) 엣지 비율 비교. 거래 그래프에서는 의심 계좌의 이웃 중 85\%가 정상 계좌이나, 행동적 k-NN 그래프에서는 S-S 비율이 41\%로 크게 증가하여 GNN의 이웃 집계가 의심 정보를 효과적으로 포착할 수 있게 된다.}
    \label{fig:intro_homophily}
\end{figure}

자금세탁은 범죄 수익의 출처를 은폐하여 합법적 자금으로 위장하는 행위로, UN은 전 세계 GDP의 2--5\%에 해당하는 규모가 매년 세탁되는 것으로 추정한다~\cite{teichmann2020recent}.
금융기관은 의심거래보고(STR) 의무를 부담하고 있으나 자금세탁 수법이 점차 정교해지면서 기존의 규칙 기반 탐지 시스템만으로는 대응에 한계가 있다~\cite{drezewski2015application}.
이에 따라 거래 네트워크의 구조적 패턴을 학습하여 의심 계좌를 식별하는 그래프 신경망(GNN) 기반 접근법이 주목받고 있다~\cite{cheng2025graph, kurshan2020graph}.

그러나 금융 거래 네트워크는 일반적인 그래프와 구별되는 두 가지 본질적 특성을 갖는다.
첫째, 저동질성(low-homophily)이다. 자금세탁 계좌는 정상 계좌와 주로 거래하므로 이웃 노드의 라벨이 유사하다는 GNN의 기본 가정이 성립하지 않는다~\cite{zhu2020beyond}.
Figure~\ref{fig:intro_homophily}에서 볼 수 있듯이, 실제 거래 그래프에서 의심 계좌의 이웃 중 85\%가 정상 계좌로 구성되어 의심 신호가 benign noise에 희석된다.
둘째, 극심한 클래스 불균형이다. 의심 계좌는 전체의 2\% 미만에 불과하여 지도학습 모델은 다수 클래스에 편향되기 쉽고, 라벨 획득 자체도 수사 및 신고 결과에 의존하여 부족하고 불완전하다~\cite{hilal2022financial}.

이러한 환경에서 그래프 대조학습(GCL)은 라벨 없이 그래프 구조로부터 표현을 학습할 수 있어 유망한 대안으로 부상하고 있다~\cite{you2020graph, zhu2020deep}.
BGRL~\cite{thakoor2021large}, GRACE~\cite{zhu2020deep}, DGI~\cite{velivckovic2018deep}, GBT~\cite{bielak2022graph} 등 다양한 GCL 모델이 제안되었으며, 
최근에는 k-NN 유사도 그래프를 contrastive view로 활용하는 MLGCL~\cite{shang2023mlgcl}과 자금세탁 탐지에 이를 적용한 GCPAL~\cite{lu2024graph}이 등장하였다.
그러나 기존 GCL 모델들은 동일 그래프에 대한 확률적 증강(edge removal, feature masking 등)으로 contrastive view를 생성하여, 저동질성 거래 네트워크 고유의 특성을 view에 반영하지 못한다.
또한 k-NN view를 도입한 최근 연구들도 두 가지 핵심적인 설계 선택에 대한 체계적 분석이 부족하다.
하나는 k-NN view를 어떤 피처로 구축해야 하는가이며, 다른 하나는 어떤 수준에서 표현을 대조해야 하는가이다.
이 두 질문에 대한 답은 GCL이 자금세탁 탐지에서 효과적으로 동작하기 위한 필수 조건이지만, 기존 연구에서는 개별적으로만 다루어졌을 뿐 통합적 분석이 이루어지지 않았다.

본 연구에서는 이러한 gap을 메우기 위해 \texttt{BECON}을 제안한다 (Figure~\ref{fig:framework}).
\texttt{BECON}은 GCL의 자금세탁 탐지 성능을 결정하는 두 독립 축, 즉 view 구성과 contrastive level을 체계적으로 분석하고 최적 조합을 도출하는 프레임워크이다.
view 구성 축에서는 노드 피처를 행동적(behavioral) 피처와 구조적(structural) 피처로 분류하고, 각 유형으로 구축한 k-NN 그래프가 contrastive view로서 얼마나 효과적인지를 비교한다.
contrastive level 축에서는 노드 수준의 대조와 서브그래프 수준의 이웃 집계가 성능에 미치는 영향을 분석한다.
이 두 축을 교차하여 4가지 설정을 구성하고, 다양한 GCL 인코더에 대해 체계적인 실험을 수행한다.
실험 결과, 행동적 k-NN view가 의심 계좌 간 연결 비율(S-S/S-B)을 1:5.7에서 1:1.4로 개선하여 동질성을 효과적으로 복원하며, 서브그래프 풀링과의 결합을 통해 추가적인 성능 향상을 달성함을 확인하였다.

금융결제원(KFTC)이 운영하는 전자금융공동망(HOFINET)의 실제 은행간 이체 데이터와 합성 AML 벤치마크(AMLworld HI-Small~\cite{amlpatterns2024realistic})에서의 실험을 통해, 
\texttt{BECON}은 기존 GCL 설정 및 지도학습 모델 대비 경쟁력 있는 탐지 성능을 달성한다. 
HOFINET은 개별 금융기관이 운영 환경에서 부여한 의심거래 라벨을 포함하며, 이를 통해 실제 AML 환경에서의 유효성을 검증한다. 
본 연구의 기여는 다음과 같다.
\begin{itemize}
    \item 행동적 k-NN view가 저동질성 거래 네트워크에서 의심 계좌 간 연결 비율을 크게 개선하여 동질성을 효과적으로 복원하는 메커니즘을 규명한다 (Section~\ref{subsec:rq1}).
    \item 서브그래프 풀링이 행동적 view와 결합할 때만 효과적이며, 저동질성 그래프에 단독 적용 시 오히려 성능이 저하되는 조건을 밝힌다 (Section~\ref{subsec:rq2}).
    \item 다양한 인코더에 대한 비교 실험을 통해 BatchNorm이 대규모 불균형 환경에서 인코더 아키텍처와 무관하게 성능을 결정하는 핵심 요인임을 발견한다 (Section~\ref{subsec:rq3}).
    \item 실제 및 합성 AML 데이터셋에서 일관된 개선 패턴을 확인하여 일반성을 검증하고 (Section~\ref{subsec:rq4}), 라벨 없이 학습한 자기지도 표현만으로 다수의 지도학습 모델과 동등한 성능을 달성함을 입증한다 (Section~\ref{subsec:rq5}).
\end{itemize}

\section{Related Work}

\section{Proposed Method}\label{sec:method}

\begin{figure*}[t!]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/fig_framework.pdf}
    \caption{\texttt{BECON} 프레임워크 개요. 거래 그래프에서 두 가지 view(증강 그래프, 행동적 k-NN)를 구성하고, 공유 GNN 인코더로 표현을 학습한 뒤 서브그래프 풀링을 적용하여 대조 손실로 학습한다. 추론 시에는 두 view의 표현을 결합하여 LogReg으로 평가한다. 하단은 두 축의 교차로 생성되는 4가지 설정을 나타내며, (d)가 제안 설정이다.}
    \label{fig:framework}
\end{figure*}

본 장에서는 \texttt{BECON} 프레임워크의 세부 구성을 기술한다. 먼저 문제를 정의한 뒤, 피처 분류 체계와 행동적 k-NN view 구축 방법을 설명하고, 이중 view 대조학습 및 서브그래프 풀링 메커니즘을 소개한다. Figure~\ref{fig:framework}는 전체 파이프라인을 도시한다.

\subsection{Problem Formulation}
거래 네트워크를 유향 그래프 $\mathcal{G}_{\text{trans}} = (\mathcal{V}, \mathcal{E}, \mathbf{X})$로 정의한다. 
여기서 $\mathcal{V}$는 계좌(노드) 집합($|\mathcal{V}|=N$), $\mathcal{E} \subseteq \mathcal{V} \times \mathcal{V}$는 이체 거래(엣지) 집합, $\mathbf{X} \in \mathbb{R}^{N \times F}$는 노드 피처 행렬이다. 
각 노드 $v_i$에는 의심 여부를 나타내는 라벨 $y_i \in \{0, 1\}$이 존재하나, 학습 단계에서는 라벨을 사용하지 않는 자기지도 설정을 따른다. 
목표는 라벨 없이 그래프 구조와 노드 피처로부터 의심 계좌를 식별할 수 있는 노드 표현 $\mathbf{Z} \in \mathbb{R}^{N \times D}$를 학습하는 것이다.

\subsection{Feature Taxonomy}\label{subsec:feature_taxonomy}
거래 네트워크에서 추출 가능한 노드 피처를 두 범주로 분류한다. 
행동적(behavioral) 피처 $\mathbf{x}_i^{\text{behav}} \in \mathbb{R}^{F_b}$는 각 계좌의 거래 패턴을 직접 반영하는 피처로, 거래 금액 통계(평균, 표준편차, 최솟값, 최댓값), 매체/자금 유형별 비율, 엔트로피 등 22개 변수로 구성된다. 
이 피처들은 자금세탁의 전형적 수법인 분할거래(structuring)나 다단계 이체(layering)의 행동적 흔적을 포착한다.
구조적(structural) 피처 $\mathbf{x}_i^{\text{struct}} \in \mathbb{R}^{F_s}$는 그래프 위상에서 파생되는 피처로, degree centrality, PageRank, HITS(hub/authority), k-core, triangle count, betweenness centrality 등 9개 변수로 구성된다. 
이 피처들은 그래프 내 노드의 위치와 역할을 정량화한다.
\texttt{BECON}은 GNN 인코더의 입력으로 행동적 피처만을 사용한다. 
구조적 피처는 GNN의 메시지 패싱을 통해 암묵적으로 학습되므로 입력에서 제외한다. 이 분류의 핵심적 역할은 k-NN view 구축에 있다.

\subsection{Behavioral k-NN View Construction}\label{subsec:knn_view}
\texttt{BECON}의 핵심 구성 요소는 행동적 유사도에 기반한 k-NN 그래프 $\mathcal{G}_{\text{knn}}$의 구축이다. 
기존 GCL 모델이 동일 그래프에 대한 확률적 증강으로 두 contrastive view를 생성하는 반면, \texttt{BECON}은 거래 그래프 $\mathcal{G}_{\text{trans}}$와 구조적으로 독립된 k-NN 그래프를 보조 view로 활용한다.

k-NN 그래프 구축 과정은 다음과 같다. 
먼저 행동적 피처 행렬 $\mathbf{X}^{\text{behav}}$에서 거래 건수(count)와 degree centrality를 제외한 순수 행동 변수만을 선별한다. 
건수와 degree는 구조적 속성과 상관이 높아 행동적 유사도의 순수성을 저해하기 때문이다. 
선별된 피처에 StandardScaler를 적용하여 정규화한 뒤, L2 정규화를 수행한다.
\begin{equation}\label{eq:l2norm}
    \tilde{\mathbf{x}}_i = \frac{\mathbf{x}_i^{\text{behav}}}{\|\mathbf{x}_i^{\text{behav}}\|_2}
\end{equation}
L2 정규화된 벡터 간 유클리드 거리는 코사인 거리에 비례하므로, ball tree 알고리즘을 활용하여 효율적으로 k-최근접 이웃을 탐색한다. 
각 노드 $v_i$에 대해 $k$개의 최근접 이웃을 연결하여 k-NN 그래프 $\mathcal{G}_{\text{knn}} = (\mathcal{V}, \mathcal{E}_{\text{knn}})$를 구축한다.
\begin{equation}\label{eq:knn_edge}
    \mathcal{E}_{\text{knn}} = \{(v_i, v_j) \mid v_j \in \text{kNN}(v_i, k)\}
\end{equation}
여기서 $\text{kNN}(v_i, k)$는 $\tilde{\mathbf{x}}_i$와의 유클리드 거리가 가장 가까운 $k$개 노드 집합이다. 본 연구에서는 $k=10$을 기본값으로 사용한다.

이 구축 방식의 핵심 통찰은 다음과 같다. 
자금세탁 계좌들은 분할거래, 반복 이체 등으로 인해 유사한 거래 금액 분포와 엔트로피 패턴을 공유한다. 
따라서 행동적 유사도 기반으로 k-NN을 구축하면 의심 계좌끼리 자연스럽게 연결되어, 거래 그래프에서 1:5.7이었던 S-S/S-B 비율이 k-NN 그래프에서는 1:1.4로 크게 개선된다 (Figure~\ref{fig:intro_homophily}). 
이로써 GNN의 이웃 집계가 의심 정보를 효과적으로 포착할 수 있게 된다.

\subsection{Dual-View Contrastive Learning}\label{subsec:dual_view}
\texttt{BECON}은 거래 그래프 $\mathcal{G}_{\text{trans}}$와 행동적 k-NN 그래프 $\mathcal{G}_{\text{knn}}$를 두 contrastive view로 활용하여 노드 표현을 학습한다.
각 view에 대해 확률적 증강을 적용한다. 
View 1은 거래 그래프에 edge removing과 feature masking을 적용한 $\tilde{\mathcal{G}}_{\text{trans}}$이고, view 2는 k-NN 그래프에 동일한 증강을 적용한 $\tilde{\mathcal{G}}_{\text{knn}}$이다. 
공유 GNN 인코더 $f_\theta$가 두 view를 각각 처리하여 노드 표현을 생성한다.
\begin{equation}\label{eq:encoding}
    \mathbf{H}^{(1)} = f_\theta(\mathbf{X}^{\text{behav}}, \tilde{\mathcal{E}}_{\text{trans}}), \quad
    \mathbf{H}^{(2)} = f_\theta(\mathbf{X}^{\text{behav}}, \tilde{\mathcal{E}}_{\text{knn}})
\end{equation}
여기서 $\mathbf{H}^{(1)}, \mathbf{H}^{(2)} \in \mathbb{R}^{N \times D}$는 각 view의 노드 임베딩이다. 
두 view가 동일한 인코더 파라미터 $\theta$를 공유함으로써, 거래 구조와 행동적 유사도라는 상호 보완적 정보를 통합하는 표현이 학습된다.

GNN 인코더는 $L$-layer GCNConv~\cite{kipf2016semi}를 기본으로 하며, 각 층에 BatchNorm, 활성 함수, dropout을 적용한다.
\begin{equation}\label{eq:gnn_layer}
    \mathbf{H}^{(l)} = \text{Dropout}\left(\sigma\left(\text{BN}\left(\hat{\mathbf{A}}\mathbf{H}^{(l-1)}\mathbf{W}^{(l)}\right)\right)\right)
\end{equation}
여기서 $\hat{\mathbf{A}}$는 정규화된 인접 행렬, $\mathbf{W}^{(l)}$은 $l$번째 층의 학습 가능한 가중치, $\sigma$는 활성 함수(PReLU 또는 ReLU), BN은 BatchNorm이다. 
Section~\ref{subsec:rq3}에서 확인하듯이, BatchNorm은 대규모 불균형 환경에서 인코더 아키텍처와 무관하게 성능을 결정하는 핵심 요인이다. 
\texttt{BECON}은 인코더 아키텍처에 비의존적으로 설계되어, GCNConv 외에도 GINConv 등 다양한 GNN 연산자를 사용할 수 있다.

대조 손실로는 BarlowTwins~\cite{zbontar2021barlow} 기반의 bootstrap loss를 사용한다. 
온라인 인코더 $f_\theta$와 모멘텀 업데이트되는 타겟 인코더 $f_\xi$를 활용하여, 네거티브 샘플 없이 표현 붕괴를 방지한다. 
각 view의 표현에 projection head $g$와 predictor $q$를 적용한 뒤, cross-view 코사인 유사도를 최대화한다.
\begin{equation}\label{eq:loss}
    \mathcal{L} = \sum_{k \in \{1,2\}} \left(2 - 2 \cdot \frac{1}{N}\sum_{i=1}^{N} \frac{q(g(\mathbf{h}_i^{(k)})) \cdot g_\xi(\mathbf{h}_{\xi,i}^{(3-k)})}{\|q(g(\mathbf{h}_i^{(k)}))\| \cdot \|g_\xi(\mathbf{h}_{\xi,i}^{(3-k)})\|}\right)
\end{equation}
여기서 $\mathbf{h}_{\xi,i}^{(\cdot)}$는 타겟 인코더의 출력이다. 타겟 인코더의 파라미터 $\xi$는 exponential moving average로 업데이트된다: $\xi \leftarrow m\xi + (1-m)\theta$, $m=0.99$.

\subsection{Subgraph Pooling}\label{subsec:subgraph_pool}
노드 수준의 대조에 더하여, \texttt{BECON}은 서브그래프 수준의 이웃 집계를 도입한다. 
각 노드 $v_i$에 대해, 자기 자신과 이웃 노드의 표현을 평균 풀링하여 서브그래프 표현을 생성한다.
\begin{equation}\label{eq:subgraph_pool}
    \mathbf{s}_i = \frac{1}{|\mathcal{N}(v_i)| + 1}\left(\mathbf{h}_i + \sum_{v_j \in \mathcal{N}(v_i)} \mathbf{h}_j\right)
\end{equation}
여기서 $\mathcal{N}(v_i)$는 $v_i$의 이웃 노드 집합이다. 이 연산은 scatter\_add를 활용하여 효율적으로 구현된다.

서브그래프 풀링은 k-NN 그래프의 이웃 구조를 기준으로 적용된다. 
이 설계의 핵심 통찰은 서브그래프 풀링이 그래프의 연결 패턴을 증폭한다는 점이다. 
행동적 k-NN 그래프에서는 의심 계좌의 이웃 대부분이 의심 계좌(S-S 비율 높음)이므로 의심 정보가 증폭되어 개선으로 이어진다. 
반면, 원래 거래 그래프에서는 의심 계좌의 이웃 대부분이 정상 계좌(S-B 비율 높음)이므로 benign noise가 증폭되어 오히려 성능이 저하된다. 
이 현상은 Section~\ref{subsec:rq2}에서 실험적으로 확인한다.

\subsection{Training and Inference}\label{subsec:training}
\texttt{BECON}의 학습 및 추론 절차를 요약한다.

학습 단계에서는 라벨을 사용하지 않고, 거래 그래프와 행동적 k-NN 그래프의 두 view에 대한 대조 손실(Eq.~\eqref{eq:loss})만으로 인코더를 최적화한다. 
각 에폭마다 두 view에 확률적 증강을 적용하고, 온라인 인코더와 타겟 인코더를 통해 표현을 생성한 뒤, bootstrap loss를 역전파한다. 타겟 인코더는 그래디언트 없이 모멘텀 업데이트만 수행한다.

추론 단계에서는 학습된 인코더의 파라미터를 고정(freeze)하고, 두 view의 노드 표현을 결합(concatenate)하여 최종 임베딩을 생성한다.
\begin{equation}\label{eq:concat}
    \mathbf{z}_i = [\mathbf{s}_i^{(1)} \| \mathbf{s}_i^{(2)}]
\end{equation}
여기서 $\mathbf{s}_i^{(1)}$과 $\mathbf{s}_i^{(2)}$는 각각 거래 그래프와 k-NN 그래프에서의 서브그래프 표현이며, $\|$는 벡터 결합을 나타낸다. 이 결합 표현에 대해 LogisticRegression 분류기를 적용하여 의심 계좌를 탐지한다.

\texttt{BECON}은 view 구성과 contrastive level의 두 독립 축을 교차하여 4가지 설정을 구성한다: (a) 증강 view + 노드 수준, (b) 행동적 k-NN view + 노드 수준, (c) 증강 view + 서브그래프, (d) 행동적 k-NN view + 서브그래프. 
정 (d)가 제안 방법의 완전한 구성이며, (a)--(c)는 각 구성 요소의 기여를 분리하여 분석하기 위한 ablation 설정이다.

\section{Experiments}

본 장에서는 제안 프레임워크를 다음의 연구 질문을 통해 평가한다.
\begin{itemize}
    \item \textbf{RQ1.} 행동적 k-NN view가 AML 탐지에 효과적인가? 그리고 왜 효과적인가?
    \item \textbf{RQ2.} 서브그래프 풀링이 추가 개선을 가져오는가? 어떤 조건에서 효과적인가?
    \item \textbf{RQ3.} 인코더 설계(BatchNorm)가 대규모 불균형 AML에서 성능에 미치는 영향은?
    \item \textbf{RQ4.} 다른 AML 데이터셋에서도 동일한 패턴이 재현되는가?
    \item \textbf{RQ5.} 지도학습 모델 대비 라벨 없는 자기지도학습이 경쟁력 있는가?
\end{itemize}

\subsection{Experimental Setting}

모든 실험은 Python 3.12, PyTorch 2.10, CUDA 12.8 환경에서 NVIDIA A100 80GB GPU 위에서 수행하였다.

\subsubsection{Datasets}

본 연구에서는 실제 및 합성 AML 데이터셋 두 가지를 사용한다.

\textbf{HOFINET.} Housing Finance Information Network(HOFINET)은 금융결제원(Korea Financial Telecommunications \& Clearings Institute, KFTC)이 운영하는 전자금융공동망의 실제 은행간 이체 데이터이다. 452,816개 계좌(노드)와 4,732,130건의 유향 멀티엣지로 구성되며, 의심 계좌 비율은 2.13\%이다. 의심거래 라벨은 개별 금융기관이 운영 환경에서 부여한 것으로, 분할거래(structuring), 신규수신처(layering) 등 6가지 AML 유형을 포함한다. 데이터 기간은 2021년 3분기부터 2024년 4분기까지 40개월이다.

\textbf{AMLworld HI-Small.} NeurIPS 2023 Datasets \& Benchmarks Track에서 공개된 합성 AML 벤치마크로, 515,088개 계좌와 5,078,345건의 유향 멀티엣지로 구성된다. Laundering 비율은 0.10\%로 HOFINET보다 20배 더 불균형하며, fan-out, scatter-gather, cycle 등 8가지 AML 패턴을 모델링한다. GCPAL 논문에서도 동일 데이터셋을 사용하여 직접 비교가 가능하다.

\subsubsection{Baselines}

\textbf{자기지도(Self-supervised).} 통합 프레임워크(subgraph\_cl.py)를 통해 10종의 인코더를 실험한다: BGRL, GBT, DGI, MVGRL, GRACE, GCA (BN 미포함) 및 DGI+BN, MVGRL+BN, GRACE+BN, GIN (BN 포함). 각 인코더에 대해 (a) augmentation view, (b) behavioral view, (c) subgraph pooling, (d) behavioral view + subgraph pooling의 4가지 설정을 교차 실험한다.

\textbf{지도학습(Supervised).} GNN 기반(GCN, GAT, GraphSAGE), 신경망(MLP), 트리 기반(LightGBM, XGBoost) 총 6종의 지도학습 모델을 비교 대상으로 사용한다. 모든 지도학습 모델에는 클래스 가중치(class weight) 기반 불균형 보정을 적용한다.

\subsubsection{Evaluation Metrics}

주요 지표로 의심 클래스(positive class)의 F1-score(F1\_susp)를 사용한다. 보조 지표로 AUROC와 AUPRC를 함께 보고한다. 모든 실험은 4개 시드(2024, 2025, 2026, 2027)의 평균으로 보고하며, 10\%/80\% train/test 분할을 사용한다. 자기지도 모델은 frozen embedding에 대해 LogisticRegression으로 평가한다.

\subsection{Effectiveness and Mechanism of Behavioral k-NN View (\textbf{RQ1})}\label{subsec:rq1}

행동적 k-NN view의 효과를 검증하기 위해 설정 (b)와 (a)를 비교한다. BN을 포함한 인코더에서 +80\%$\sim$151\%의 F1\_susp 개선을 달성하였으며, BN이 없는 인코더에서도 +24\%$\sim$31\%의 일관된 개선을 보인다.

이러한 효과의 메커니즘을 분석하기 위해 각 그래프의 Suspicious-to-Suspicious/Suspicious-to-Benign(S-S/S-B) 연결 비율을 측정하였다. 원래 거래 그래프에서는 S-S/S-B = 1:5.7로 의심 계좌가 정상 계좌에 묻혀 있으나, 행동적 k-NN 그래프에서는 1:1.4로 크게 개선된다. 이는 자금세탁 계좌들이 분할거래(structuring), 반복이체(layering) 등으로 인해 유사한 거래 금액 패턴을 공유하기 때문이며, 행동적 유사도 기반 k-NN 구축 시 의심 계좌끼리 자연스럽게 연결되어 GNN의 이웃 집계(neighborhood aggregation)가 의심 정보를 효과적으로 수집할 수 있게 된다.

\subsection{Subgraph Pooling: When Does It Help? (\textbf{RQ2})}\label{subsec:rq2}

서브그래프 풀링의 효과는 결합 조건에 따라 상반된 결과를 보인다. 행동적 k-NN view 위에 서브그래프 풀링을 추가한 설정 (d)는 (b) 대비 소폭 추가 개선을 달성한다. 반면, 원래 거래 그래프에서 서브그래프 풀링만 적용한 설정 (c)는 (a) 대비 오히려 -19\%$\sim$25\% 성능이 저하된다.

이는 서브그래프 풀링이 이웃 정보를 명시적으로 집계하여 그래프의 연결 패턴을 증폭하는 특성에 기인한다. 행동적 k-NN 그래프(S-S/S-B 1:1.4)에서는 의심 이웃 정보가 증폭되어 개선으로 이어지지만, 원래 거래 그래프(S-S/S-B 1:5.7)에서는 benign 이웃의 noise가 증폭되어 오히려 악화된다. 이 결과는 view 구성이 선행되어야 서브그래프 풀링이 효과적임을 시사한다.

\subsection{The Decisive Role of BatchNorm (\textbf{RQ3})}\label{subsec:rq3}

10종 인코더에 대한 비교 실험 결과, BatchNorm(BN)의 유무가 성능을 결정하는 가장 핵심적인 요인임을 발견하였다. BN을 포함한 인코더(GBT, DGI+BN, MVGRL+BN, GRACE+BN)는 F1\_susp 0.681$\sim$0.682를 달성한 반면, BN이 없는 인코더(DGI, MVGRL, GRACE, GCA)는 0.069$\sim$0.085에 그쳤다. BN 추가만으로 약 10배의 성능 향상을 얻을 수 있다.

세부적으로, per-layer BN(GBT 등, 0.682)이 final-only BN(BGRL, 0.647)보다 우수하며, GCNConv(0.682)가 GINConv(0.570)보다 효과적이다. 반면 활성함수(PReLU vs ReLU)나 Dropout 유무는 부차적이다. 이는 452K 노드, 2\% suspicious의 대규모 불균형 환경에서 BN이 학습 안정화에 필수적이며, BN 없이는 표현이 다수 클래스(benign)에 collapse됨을 의미한다.

\subsection{Generalizability Across AML Datasets (\textbf{RQ4})}\label{subsec:rq4}

AMLworld HI-Small 데이터셋에서도 (d) $>$ (b) $>$ (a) $>$ (c)의 패턴이 동일하게 재현된다. BGRL 인코더 기준 설정 (d)에서 F1\_susp 0.068을 달성하여 baseline (a) 대비 +81\%의 개선을 보인다.

HOFINET 대비 절대 성능이 낮은 원인은 AMLworld의 laundering 비율이 0.10\%로 HOFINET(2.13\%)보다 20배 더 불균형하고, S-S/S-B 비율이 1:15.8로 HOFINET(1:5.7)보다 3배 더 희석되어 있기 때문이다. 행동적 k-NN이 S-S/S-B를 1:9.4로 개선하지만 HOFINET(1:1.4)만큼 효과적이지는 않다. 그러나 상대적 개선 패턴은 데이터셋에 독립적으로 일관되게 나타나, 제안 방법의 일반성을 확인할 수 있다.

\subsection{Comparison with Supervised Models (\textbf{RQ5})}\label{subsec:rq5}

HOFINET에서 제안 방법의 설정 (d)는 F1\_susp 0.682를 달성하여, 라벨을 사용한 지도학습 모델인 MLP(0.678), GraphSAGE(0.677), XGBoost(0.675), LightGBM(0.674)과 동등한 성능을 보인다. 반면 GCN(0.250)과 GAT(0.469)는 상대적으로 낮은 성능을 기록하였다.

이 결과는 라벨 없이 학습한 자기지도 표현만으로도 완전 지도학습과 동등한 AML 탐지 성능을 달성할 수 있음을 보여준다. AML 도메인에서 라벨 획득은 수사 및 신고 결과에 의존하여 부족하고 불완전한 경우가 대부분이므로, 자기지도 방법의 실용적 가치가 높다.

\section{Conclusion and Future Work}

\clearpage
\bibliographystyle{ACM-Reference-Format}
\bibliography{aml}

\end{document}
